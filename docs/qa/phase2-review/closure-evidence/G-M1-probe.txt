G-M1 — /health/live liveness endpoint
======================================
Date: 2026-06-20
Status: STATIC-PROVEN (live SQL unreachable on dev machine — DB connection string targets
        DESKTOP-FB2ES22\SQL2022 which is not available in this session).

Code evidence (Program.cs lines 147-153):

    // G-M1: Liveness endpoint — no dependency checks; returns 200 whenever the process is alive.
    // Predicate = _ => false means no registered checks are executed, so the aggregate result
    // is always Healthy regardless of DB state.  Safe to call from load-balancer without a DB.
    app.MapHealthChecks("/health/live", new Microsoft.AspNetCore.Diagnostics.HealthChecks.HealthCheckOptions
    {
        Predicate = _ => false
    }).AllowAnonymous();

Behaviour contract:
  - Predicate = _ => false  →  no registered IHealthCheck instances are executed
  - ASP.NET Core health-check middleware aggregates an empty entry set → status = Healthy
  - HTTP 200 (default ResultStatusCodes for Healthy)
  - AllowAnonymous() ensures the [Authorize] middleware added in Program.cs does NOT gate this
    endpoint, so it is reachable without a session cookie (load-balancer / k8s probe safe).
  - /health/ready (lines 136-145) retains Predicate = check => check.Tags.Contains("ready"),
    which includes the "prisma-db" PrismaDbHealthCheck → returns 503 when DB is down.

DoD verification:
  [x] Anon GET /health/live → 200 even when DB is down  (predicate excludes all checks)
  [x] /health/ready → 503 when DB down                   (prisma-db check tagged "ready")
  [x] Build 0/0 errors 0/0 warnings                      (confirmed below)

Build result:
  dotnet build ExxerCube.Prisma.Web.UI.csproj --no-incremental
  Build succeeded. 0 Warning(s) 0 Error(s)
