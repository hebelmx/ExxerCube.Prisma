# AdaptiveTxtFieldExtractor - Comprehensive Field Catalog
## Field Extraction Requirements from OCR Text

**Created**: 2025-12-10
**Purpose**: Catalog all extractable fields from PDF OCR text for 3-way reconciliation
**Status**: 🔴 ANALYSIS COMPLETE - Ready for Implementation

---

## 📊 Executive Summary

### Current State (BEFORE Enhancement)
- **Fields Extracted**: 2/40+ (5% coverage)
- **Success Rate**: Only `NumeroOficio` and `AutoridadNombre` working
- **Missing**: 38+ critical fields including `Expediente`, `Causa`, contact info, addresses, legal basis

### Target State (AFTER Enhancement)
- **Fields Extracted**: 20-25/40+ (50-60% coverage)
- **Success Rate**: All high-priority fields extractable
- **Coverage**: Core Expediente properties + SolicitudPartes basics + contact info

---

## 🎯 Field Priority Matrix

### Priority 1: CRITICAL (Must Extract - 3-Way Reconciliation Blockers)
These fields are **required** for 3-way reconciliation and R29 compliance:

| Field | XML Property | Expected in OCR? | Extraction Difficulty | Notes |
|-------|--------------|------------------|----------------------|-------|
| NumeroOficio | `Cnbv_NumeroOficio` | ✅ YES | 🟢 EASY | Pattern: `[A-Z0-9]+/\d{4}/\d{6}` |
| NumeroExpediente | `Cnbv_NumeroExpediente` | ❌ NO | 🔴 HARD | **NOT in OCR text** - Only in XML |
| SolicitudSiara | `Cnbv_SolicitudSiara` | ✅ YES | 🟢 EASY | Same as NumeroOficio in these fixtures |
| AutoridadNombre | `AutoridadNombre` | ✅ YES | 🟡 MEDIUM | Long authority names, variable formats |
| AreaDescripcion | `Cnbv_AreaDescripcion` | ❌ NO | 🔴 HARD | Not explicitly labeled in OCR |
| FechaPublicacion | `Cnbv_FechaPublicacion` | ⚠️ MAYBE | 🔴 HARD | May be in document body, not header |
| DiasPlazo | `Cnbv_DiasPlazo` | ⚠️ MAYBE | 🔴 HARD | May be in instructions text |
| TieneAseguramiento | `TieneAseguramiento` | ⚠️ MAYBE | 🟡 MEDIUM | Derive from keywords: "aseguramiento", "embargo", "bloqueo" |

### Priority 2: HIGH (Should Extract - Data Completeness)
These fields significantly improve reconciliation confidence:

| Field | XML Property | Expected in OCR? | Extraction Difficulty | Notes |
|-------|--------------|------------------|----------------------|-------|
| NombreSolicitante | `NombreSolicitante` | ✅ YES | 🟡 MEDIUM | Name after job title (e.g., "Mtro. Guadalupe Pepita Pepita") |
| AutoridadEspecificaNombre | `AutoridadEspecificaNombre` | ✅ YES | 🟡 MEDIUM | Full authority name in header |
| Email | N/A (additional) | ✅ YES | 🟢 EASY | Pattern: `[a-z0-9.]+@[a-z0-9.]+` |
| Telefono | N/A (additional) | ✅ YES | 🟢 EASY | Pattern: `\(\d{2}\)\s*\d{4}-\d{4}` |
| Direccion | N/A (additional) | ✅ YES | 🟡 MEDIUM | Multi-line address block |
| CodigoPostal | N/A (additional) | ✅ YES | 🟢 EASY | Pattern: `C\.?P\.?\s*\d{5}` |
| FundamentoLegal | `FundamentoLegal` | ✅ YES | 🟡 MEDIUM | Legal article references (Art. 142, etc.) |
| Referencia | `Referencia` | ⚠️ MAYBE | 🔴 HARD | Varies widely by document type |
| Referencia1 | `Referencia1` | ⚠️ MAYBE | 🔴 HARD | Additional references |
| Referencia2 | `Referencia2` | ⚠️ MAYBE | 🔴 HARD | Additional references |

### Priority 3: MEDIUM (Nice to Have - Enhanced Context)
These fields provide additional context but aren't critical:

| Field | XML Property | Expected in OCR? | Extraction Difficulty | Notes |
|-------|--------------|------------------|----------------------|-------|
| Folio | `Cnbv_Folio` | ❌ NO | 🔴 IMPOSSIBLE | Generated by system, not in OCR |
| OficioYear | `Cnbv_OficioYear` | ✅ YES | 🟢 EASY | Extract from NumeroOficio |
| AreaClave | `Cnbv_AreaClave` | ❌ NO | 🔴 IMPOSSIBLE | Catalog code, not in OCR |
| Subdivision | `Subdivision` | ❌ NO | 🔴 IMPOSSIBLE | Derived field |
| MedioEnvio | `MedioEnvio` | ⚠️ MAYBE | 🔴 HARD | "SIARA" or "Físico" - may be implicit |

### Priority 4: LOW (Complex Nested Data - Future Enhancement)
These require structured extraction from document body:

| Field | XML Property | Expected in OCR? | Extraction Difficulty | Notes |
|-------|--------------|------------------|----------------------|-------|
| SolicitudPartes | `SolicitudPartes` | ✅ YES | 🔴 VERY HARD | Nested: Names, RFC, Carácter |
| PersonasSolicitud | `PersonasSolicitud` | ✅ YES | 🔴 VERY HARD | Nested: Names, RFC, Domicilio, CURP |
| InstruccionesCuentasPorConocer | `InstruccionesCuentasPorConocer` | ✅ YES | 🔴 VERY HARD | Long text block, variable format |

---

## 📋 Field-by-Field Extraction Specifications

### 1. NumeroOficio ✅ WORKING
**XML Examples**:
- `222/AAA/-4444444444/2025`
- `333/BBB/-4444444444/2025`
- `AGAFADAFSON2/2025/000084`

**OCR Location**: Line 4 - "No. De Identificación del Requerimiento: AGAFADAFSON2/2025/000084"

**Current Pattern** (WORKING):
```regex
# Primary pattern
[A-Z]{4,}[A-Z0-9]{0,10}/\d{4}/\d{6}

# Labeled pattern
(?:No\.\s*De\s*Identificaci[óo]n|N[úu]mero\s*de\s*Oficio)\s*[:：]?\s*([A-Z0-9/]+)
```

**Confidence**: 🟢 HIGH (already extracting correctly)

---

### 2. NumeroExpediente ❌ NOT IN OCR
**XML Examples**:
- `A/AS1-1111-222222-AAA`
- `H/IN1-1111-222222-AAA`
- `A/AS1-4444-5555555-HHHH`
- `E/DE-3333-4444444-AAA`

**OCR Location**: ❌ **NOT PRESENT** in OCR text (only in XML)

**Root Cause**: The official PDF documents do NOT contain the Expediente number in the header or body. This is a **CNBV internal tracking number** generated after document receipt.

**Extraction Strategy**:
- ❌ Cannot extract from PDF OCR
- ✅ Must come from XML source
- ⚠️ Fusion service should prioritize XML for this field

**Confidence**: 🔴 IMPOSSIBLE from OCR

---

### 3. SolicitudSiara ✅ CAN EXTRACT
**XML Examples**:
- `AGAFADAFSON2/2025/000084` (same as NumeroOficio in these fixtures)

**OCR Location**: Line 4 (same as NumeroOficio)

**Pattern** (same as NumeroOficio):
```regex
[A-Z]{4,}[A-Z0-9]{0,10}/\d{4}/\d{6}
```

**Confidence**: 🟢 HIGH (identical to NumeroOficio in test fixtures)

---

### 4. AutoridadNombre ✅ WORKING
**XML Examples**:
- `SUBDELEGACION 8 SAN ANGEL`
- `ADMINISTRACION DESCONCENTRADA DE AUDITORIA FISCAL DE SONORA "2"`
- `JUZGADO CUARTO DE LO MERCANTIL...`
- `UNIDAD DE INTELIGENCIA FINANCIERA`

**OCR Location**: Lines 1-2, 7, 12-18 (variable locations)

**Current Pattern** (WORKING):
```csharp
var authorities = new[]
{
    "Comisión Nacional Bancaria y de Valores",
    "Administración General de Auditoría Fiscal Federal",
    "AGAFF", "CNBV", "SAT"
};
```

**Enhancement Needed**: Add 200+ authority names from entity catalog (see `ENTITY_EXTRACTION_METHODOLOGY.md`)

**Confidence**: 🟡 MEDIUM (works for known authorities, misses unknowns)

---

### 5. AutoridadEspecificaNombre ✅ CAN EXTRACT
**XML Example**: `DIRECCION GENERAL DE LO CONTENCIOSO` (555CCC fixture)

**OCR Location**: Lines 12-14 (specific department/office name)

**New Pattern**:
```regex
# Look for "Direccion", "Departamento", "Mesa", "Turno", "Secretaría"
(?:Direcci[óo]n|Departamento|Mesa|Turno|Secretar[íi]a)\s+(?:General|Especial|de|Del)?\s+[A-Z\s]+
```

**Implementation**:
```csharp
private static string? ExtractAutoridadEspecifica(string text)
{
    var pattern = @"(?:DIRECCI[ÓO]N|DEPARTAMENTO|MESA|TURNO|SECRETAR[ÍI]A)\s+(?:GENERAL|ESPECIAL|DE|DEL)?\s+[A-Z\s]+";
    var match = Regex.Match(text, pattern, RegexOptions.IgnoreCase);
    return match.Success ? match.Value.Trim() : null;
}
```

**Confidence**: 🟡 MEDIUM

---

### 6. NombreSolicitante ✅ CAN EXTRACT
**OCR Location**: Line 15 - "Mtro. Guadalupe Pepita Pepita"

**Pattern Strategy**: Look for honorific titles + name
```regex
# Honorific patterns
(?:Mtro\.|Mtra\.|Lic\.|Ing\.|Dr\.|Dra\.|C\.)\s+([A-ZÁÉÍÓÚÑ][a-záéíóúñ]+(?:\s+[A-ZÁÉÍÓÚÑ][a-záéíóúñ]+)+)

# Or job title + name
(?:Vicepresidente|Director|Administrador|Secretario|Titular)\s+(?:de|del)?\s+[^\\n]+\\n([A-ZÁÉÍÓÚÑ].+)
```

**Implementation**:
```csharp
private static string? ExtractNombreSolicitante(string text)
{
    // Try honorific pattern first
    var honorificPattern = @"(?:Mtro\.|Mtra\.|Lic\.|Ing\.|Dr\.|Dra\.|C\.)\s+([A-ZÁÉÍÓÚÑ][a-záéíóúñ]+(?:\s+[A-ZÁÉÍÓÚÑ][a-záéíóúñ]+)+)";
    var match = Regex.Match(text, honorificPattern);
    if (match.Success && match.Groups.Count > 1)
    {
        return match.Groups[1].Value.Trim();
    }

    // Try job title pattern
    var titlePattern = @"(?:Vicepresidente|Director|Administrador|Secretario|Titular)\s+(?:de|del)?\s+[^\n]+\n([A-ZÁÉÍÓÚÑ].+)";
    match = Regex.Match(text, titlePattern);
    if (match.Success && match.Groups.Count > 1)
    {
        var lines = match.Groups[1].Value.Split('\n');
        return lines[0].Trim(); // First line after title
    }

    return null;
}
```

**Confidence**: 🟡 MEDIUM

---

### 7. Email ✅ CAN EXTRACT
**OCR Location**: Line 23 - "lupita.pepita.pepita@sat.gob.mx"

**Pattern**:
```regex
[a-z0-9._-]+@[a-z0-9.-]+\.[a-z]{2,}
```

**Implementation**:
```csharp
private static string? ExtractEmail(string text)
{
    var emailPattern = @"[a-z0-9._-]+@[a-z0-9.-]+\.[a-z]{2,}";
    var match = Regex.Match(text, emailPattern, RegexOptions.IgnoreCase);
    return match.Success ? match.Value.ToLower() : null;
}
```

**Confidence**: 🟢 HIGH

---

### 8. Telefono ✅ CAN EXTRACT
**OCR Location**: Line 21 - "Tel. (55) 1111-2222"

**Pattern**:
```regex
# Pattern 1: (55) 1111-2222
\(?\d{2}\)?\s*\d{4}-\d{4}

# Pattern 2: Tel. / Teléfono: labeled
(?:Tel[ée]fono|Tel\.?)\s*[:：]?\s*(\(?\d{2}\)?\s*\d{4}-\d{4})
```

**Implementation**:
```csharp
private static string? ExtractTelefono(string text)
{
    var labeledPattern = @"(?:Tel[ée]fono|Tel\.?)\s*[:：]?\s*(\(?\d{2}\)?\s*\d{4}-\d{4})";
    var match = Regex.Match(text, labeledPattern, RegexOptions.IgnoreCase);
    if (match.Success && match.Groups.Count > 1)
    {
        return match.Groups[1].Value.Trim();
    }

    // Fallback: just find phone pattern
    var phonePattern = @"\(?\d{2}\)?\s*\d{4}-\d{4}";
    match = Regex.Match(text, phonePattern);
    return match.Success ? match.Value : null;
}
```

**Confidence**: 🟢 HIGH

---

### 9. Direccion ✅ CAN EXTRACT
**OCR Location**: Lines 8-9, 20-22 (multi-line address blocks)

**Pattern Strategy**: Extract multi-line block containing address keywords

**Implementation**:
```csharp
private static string? ExtractDireccion(string text)
{
    // Look for lines containing street, col, cp, ciudad
    var addressPattern = @"([^\n]*(?:Av\.|Ave\.|Calle|Col\.|Colonia|C\.P\.|CP|Ciudad|Del\.|Delegaci[óo]n|Municipio)[^\n]*(?:\n[^\n]*){0,3})";
    var match = Regex.Match(text, addressPattern, RegexOptions.IgnoreCase);
    if (match.Success)
    {
        return match.Value.Trim().Replace("\n", " ");
    }
    return null;
}
```

**Confidence**: 🟡 MEDIUM (multi-line extraction is tricky)

---

### 10. CodigoPostal ✅ CAN EXTRACT
**OCR Location**: Line 9 - "C.P. 01020", Line 22 - "CP 00001"

**Pattern**:
```regex
C\.?P\.?\s*(\d{5})
```

**Implementation**:
```csharp
private static string? ExtractCodigoPostal(string text)
{
    var cpPattern = @"C\.?P\.?\s*(\d{5})";
    var match = Regex.Match(text, cpPattern, RegexOptions.IgnoreCase);
    return match.Success && match.Groups.Count > 1
        ? match.Groups[1].Value
        : null;
}
```

**Confidence**: 🟢 HIGH

---

### 11. FundamentoLegal ✅ CAN EXTRACT
**OCR Location**: Lines 27-33 - Legal article references

**Pattern Strategy**: Extract article citations

**Implementation**:
```csharp
private static List<string> ExtractFundamentoLegal(string text)
{
    var articulos = new List<string>();

    // Pattern: "artículo 142", "Art. 251", "artículos 1, 2 y 3"
    var articuloPattern = @"art[íi]culos?\s+(\d+(?:\s*(?:y|,)\s*\d+)*(?:\s+fracci[óo]n\s+[IVXLCDM]+)?(?:\s+de\s+.+?(?:ley|reglamento|código))?)[,\.]?";
    var matches = Regex.Matches(text, articuloPattern, RegexOptions.IgnoreCase);

    foreach (Match match in matches)
    {
        if (match.Groups.Count > 1)
        {
            articulos.Add($"Art. {match.Groups[1].Value.Trim()}");
        }
    }

    return articulos;
}
```

**Confidence**: 🟡 MEDIUM

---

### 12. FechaPublicacion ⚠️ MAYBE CAN EXTRACT
**XML Examples**: `2025-06-05`, `2025-06-04`

**OCR Location**: ❓ May not be explicitly labeled

**Pattern Strategy**: Look for date patterns in header or near "Fecha de Publicación" label

**Implementation**:
```csharp
private static DateTime? ExtractFechaPublicacion(string text)
{
    // Try labeled pattern first
    var labeledPattern = @"(?:Fecha\s+de\s+Publicaci[óo]n|Publicado)\s*[:：]?\s*(\d{1,2}[/-]\d{1,2}[/-]\d{2,4})";
    var match = Regex.Match(text, labeledPattern, RegexOptions.IgnoreCase);

    if (match.Success && match.Groups.Count > 1)
    {
        if (DateTime.TryParse(match.Groups[1].Value, out var fecha))
        {
            return fecha;
        }
    }

    return null; // Don't guess - only extract if explicitly labeled
}
```

**Confidence**: 🔴 LOW (may not be present in OCR)

---

### 13. DiasPlazo ⚠️ MAYBE CAN EXTRACT
**XML Examples**: `7`, `10`, `3`, `1`

**OCR Location**: ❓ May be in instruction text

**Pattern Strategy**: Look for "X días" or "plazo de X días"

**Implementation**:
```csharp
private static int? ExtractDiasPlazo(string text)
{
    // Pattern: "plazo de 7 días", "en 10 días", "dentro de 3 días"
    var plazoPattern = @"(?:plazo\s+de|en|dentro\s+de)\s+(\d{1,3})\s+d[íi]as?";
    var match = Regex.Match(text, plazoPattern, RegexOptions.IgnoreCase);

    if (match.Success && match.Groups.Count > 1)
    {
        if (int.TryParse(match.Groups[1].Value, out var dias))
        {
            return dias;
        }
    }

    return null;
}
```

**Confidence**: 🟡 MEDIUM

---

### 14. TieneAseguramiento ⚠️ CAN DERIVE
**XML Examples**: `true`, `false`

**OCR Location**: Can derive from keywords in document body

**Pattern Strategy**: Keyword detection

**Implementation**:
```csharp
private static bool? ExtractTieneAseguramiento(string text)
{
    var aseguramientoKeywords = new[]
    {
        "aseguramiento",
        "embargo",
        "bloqueo",
        "retenci[óo]n",
        "inmovilizaci[óo]n",
        "congelamiento"
    };

    foreach (var keyword in aseguramientoKeywords)
    {
        if (Regex.IsMatch(text, keyword, RegexOptions.IgnoreCase))
        {
            return true;
        }
    }

    // Check for desbloqueo/liberación keywords (indicates removal of seizure)
    var desbloqueoKeywords = new[] { "desbloqueo", "liberaci[óo]n", "dejar sin efectos" };
    foreach (var keyword in desbloqueoKeywords)
    {
        if (Regex.IsMatch(text, keyword, RegexOptions.IgnoreCase))
        {
            return false;
        }
    }

    return null; // Cannot determine
}
```

**Confidence**: 🟡 MEDIUM

---

## 📊 Extraction Coverage Summary

### By Priority Level

| Priority | Total Fields | Extractable | Extraction Rate |
|----------|-------------|-------------|-----------------|
| P1 (Critical) | 8 | 3-5 | 37-62% |
| P2 (High) | 10 | 8-9 | 80-90% |
| P3 (Medium) | 5 | 1-2 | 20-40% |
| P4 (Low - Nested) | 3 | 0 | 0% (future) |
| **TOTAL** | **26** | **12-16** | **46-61%** |

### Current vs Target

| Metric | Current (Before) | Target (After) | Improvement |
|--------|------------------|----------------|-------------|
| Fields Extracted | 2 | 12-16 | **6-8x** |
| Extraction Rate | 5% | 46-61% | **+56%** |
| P1 Coverage | 25% (2/8) | 62% (5/8) | **+37%** |
| P2 Coverage | 0% (0/10) | 85% (8.5/10) | **+85%** |

---

## 🎯 Implementation Roadmap

### Phase A1: Core Field Enhancement (Priority 1)
**Target**: Get to 62% P1 coverage

- [x] NumeroOficio (already working)
- [x] AutoridadNombre (already working)
- [ ] SolicitudSiara (use NumeroOficio pattern)
- [ ] FechaPublicacion (labeled pattern)
- [ ] DiasPlazo (keyword + number pattern)
- [ ] TieneAseguramiento (keyword detection)

### Phase A2: Contact & Address Fields (Priority 2)
**Target**: Get to 85% P2 coverage

- [ ] NombreSolicitante (honorific + name pattern)
- [ ] Email (standard email regex)
- [ ] Telefono (Mexican phone format)
- [ ] Direccion (multi-line address block)
- [ ] CodigoPostal (CP pattern)
- [ ] FundamentoLegal (article citations)
- [ ] AutoridadEspecificaNombre (department/office pattern)

### Phase A3: Authority Catalog Integration
**Target**: Improve AutoridadNombre to 95% coverage

- [ ] Load 200+ authority names from entity catalog
- [ ] Implement fuzzy matching for authority names
- [ ] Add abbreviation expansion (e.g., SAT → Servicio de Administración Tributaria)

### Phase A4: Edge Cases & OCR Error Handling
**Target**: Handle real-world OCR imperfections

- [ ] Character substitution cleanup (O→0, I→1, l→1)
- [ ] Variable spacing normalization
- [ ] Accent mark tolerance (ó vs o)
- [ ] Multi-line field assembly

---

## 📚 References

- **Expediente Entity**: `Domain/Entities/Expediente.cs`
- **XML Fixtures**: `Prisma/Fixtures/PRP1/*.xml` (4 fixtures)
- **OCR Fixture**: `Prisma/Fixtures/PRP1/222AAA-44444444442025_page-0001.ocr.txt`
- **Entity Catalog**: `docs/AAA Initiative Design/ENTITY_EXTRACTION_METHODOLOGY.md`
- **Implementation Plan**: `docs/sessions/Unified-3Way-Reconciliation-Implementation-Plan.md`

---

**Last Updated**: 2025-12-10
**Status**: 🔴 READY FOR PHASE C (Documentation Update) and PHASE A (Implementation)
**Next Action**: Update Unified-3Way-Reconciliation-Implementation-Plan.md with comprehensive requirements
