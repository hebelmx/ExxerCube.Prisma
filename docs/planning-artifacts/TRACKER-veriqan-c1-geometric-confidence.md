# TRACKER — Veriqan C1: geometric extraction-confidence

**Branch:** `Liv` · **Started:** 2026-07-16 · **Design of record:** `SCOPING-veriqan-c1-geometric-extraction-confidence.md` (§"C1 intended-solution design")
**Orchestrator scope (owner-confirmed 2026-07-16):** Full C1 epic (C1.0a → C1.5) in one supervised loop. The **C1.0b spike is a hard internal gate**: if `margin < 0.15` or any clean field `< 0.8`, HALT and surface to owner — never lower the floor / shop the threshold.

## The lever (one line)
Money/rate fields are text-layer (no digit noise); the real misread is GEOMETRIC (wrong token/column/row). Emit a geometric-plausibility confidence from the positional extractor; the already-wired 0.8 guard (15 rules) converts a low-plausibility pick into honest `InsufficientData`. **No rule changes.**

## Load-bearing design facts (party, do not re-derive)
- **`sin IVA` sibling/order-marker is THE load-bearing signal** for the CAT/TASA swap. Token-competition COUNT is structurally inert for the swap (`pctTokens.Count` always 2) — it only catches a spurious *extra* decoy token.
- **MULTIPLICATIVE penalty** formula (any single red flag drags <0.8 alone), NOT weighted sum.
- Constants in static internal `FieldCalibrationTable`, NOT tenant config. 0.8 threshold untouched.
- Gap-distance signal (#4) is **CUT**. Column signal (#3) is **binary dual-pass disagreement**, not continuous distance.
- Corpus has ZERO adversarial specimens today → generator (C1.0a) is a **prerequisite**, not the final increment.

## Baseline test counts (post-C1.0b — new floor for "bit-identical" gates)
Extraction **370** (366 +4 golden roundtrip cases) · Validation **532** · Orchestration **165** (147 +4 C1.0a, +14 C1.0b spike) · Application **158**. Run via bare `net10.0/` DLL (freshest output dir), not `Debug/net10.0/`.

## Post-C1.2 test counts (new floor)
Extraction **398** (370 +28: 12 `GeometricPlausibilityScorerTests` unit + 16 `GeometricPlausibilityCalibrationTests` production-wiring calibration) · Validation **532** (unchanged) · Orchestration **165** (unchanged) · Application **158** (unchanged). Solution build 0/0.

## Post-C1.3 test counts (new floor) — SHIPPED DARK (owner ruling)
Extraction **398** (unchanged) · Validation **532** (unchanged) · Orchestration **171** (165 +6 `GeometricConfidenceArmingGateE2ETests`: 5-demo verdict-diff `[Theory]` + 1 `s-c1-swap-displaced` negative-control `[Fact]`) · Application **158** (unchanged). Solution build 0/0.
**Production default = OFF/DARK** (`PdfExtractionOptions.EmitGeometricConfidence = false`); killswitch/arm = `Veriqan:PdfExtraction:EmitGeometricConfidence` config key (set `true` to arm once real Tasa/Cat data validates).

## Post-C1.4 test counts (new floor) — SHIPPED DARK, ONE shared flag with Tasa/Cat
Extraction **420** (398 +22: 11 `GeometricPlausibilityScorerTests` RESUMEN unit cases +
11 `GeometricPlausibilityResumenCalibrationTests`/golden-roundtrip: 9 calibration `[Theory]`/`[Fact]`
cases + 2 index-driven golden-roundtrip `[Theory]` cases for the 2 new corpus specimens) ·
Validation **532** (unchanged) · Orchestration **178** (171 +6 `GeometricConfidenceResumenArmingGateE2ETests`:
5-demo verdict-diff `[Theory]` + 1 `decoy-resumen-amount` CL-21 negative-control `[Fact]`, +1 net from
existing index-driven Honesty theories auto-picking up the 2 new corpus specimens) · Application **158**
(unchanged). Solution build 0/0. **`PdfExtractionOptions.EmitGeometricConfidence` stays false by
default — confirmed the ONE flag now gates both Tasa/Cat (`ExtractTasaAndCat`) and 7 of the 11 RESUMEN
fields (`ExtractResumenField`); C1.3's doc comment claiming "default true" was stale (the actual C#
property has no initializer → runtime default `false`) — fixed as part of C1.4 (see
`PdfExtractionOptions.cs`/`VeriqanExtractionExtensions.cs`).**
**⚠️ THE C1.3 FINDING (owner-decided ship-dark 2026-07-16):** on all 5 REAL demo fixtures, `ExtractTasaAndCat` does NOT match the layout → Tasa/Cat are `NotExtracted` (0.0 both flag states). So the 5-demo verdict-diff gate passed **vacuously** for Tasa/Cat — the scorer's false-abstain safety is proven ONLY on synthetic fixtures, never on real extracted Tasa/Cat. The demo-diff test is correctly `[Category=LiveOcr]` (the demo pipeline invokes E7 header OCR) → CI-excluded; the **armed-flip scorer proof runs in CI via `GeometricPlausibilityCalibrationTests`** (Extraction.Tests, in the 398: `s-c1-swap-displaced` armed → conf <0.8). **Implication for C1.4:** RESUMEN fields DO extract on the real demo (CL-21/CL-44 fire on `bad-math-cl21.pdf`) → C1.4's verdict-diff gate will be NON-vacuous; C1.4 is where the lever meets real demo data.

## ✅ C1.0b SPIKE VERDICT = GO (settled mechanism for C1.2 to inherit)
Margin **0.35** (min clean 1.000 − max ambiguous 0.650 ≥ 0.15), proven on a train/holdout split, robust to ±3pt jitter (50 trials). **Signals (multiplicative, independent):** #1 **sibling/order-marker rank-adjacency** — is `sin`+`IVA` the two words immediately after the CAT-picked token in Left-order, before the TASA pick? (LOAD-BEARING; catches the marker-displaced swap). #2 **competition count >2** (decoy only; inert for the swap). **Constants:** `SiblingAbsentPenalty=0.55`, `CompetitionExcessPenalty=0.65`. **Settled: penalty-factor, NOT gate** (decoy-percent's `band:low` ground truth rules out a gate). **C1.2 MUST use rank/ordinal adjacency, not edge-distance** (edge-gap ~2.2pt < ±3pt jitter → memorized/fragile). Prototype: `Calibration/GeometricPlausibilityScorerPrototype.cs` + `SpikeBandLocator.cs` + `C1_0b_SeparationSpikeTests.cs` (14 tests).
**⚠️ CARRY-FORWARD for C1.3:** the lever catches *marker-displaced* swaps (`s-c1-swap-displaced`), NOT the geometrically-invisible central-marker swap (`s-c1-swap`) — an inherent, documented blind spot. C1.3's negative control MUST use **`s-c1-swap-displaced`** (which flips → InsufficientData when armed); `s-c1-swap` must NOT be expected to flip.

## Status
| # | Story | Status | Verify gate |
|---|---|---|---|
| C1.0a | Generator adversarial specimens + manifest ground truth | ✅ done (`f37a60b2`) | `s-c1-swap` proven confident-WRONG through live pipeline (CL-10 flips Pass→confident Fail) — VERIFIED |
| C1.0b | Separation spike (MAKE-OR-BREAK) | ✅ GO (`9e6c9e03`) | margin 0.35, train/holdout blind, ±3pt robust — VERIFIED (read prototype + reran 14 tests) |
| C1.1 | `ExtractedField.Found` confidence overload | ✅ done (`4af169e5`) | bit-identical green — VERIFIED |
| C1.2 | `GeometricPlausibilityScorer` + signals in `ExtractTasaAndCat` (DARK) | ✅ done (`64f40192`) | scorer unit (28 new) + calibration green; flag-off byte-identical (398/532/165/158) — VERIFIED |
| C1.3 | Verdict-diff harness + killswitch seam; SHIPPED DARK (owner) | ✅ done (`4aac943e`) | harness 6 tests green; default OFF; gate vacuous on real Tasa/Cat (synthetic-only proof) — VERIFIED (clean rebuild) |
| C1.4 | Extend to `ScanResumenColumn` (7 of 11 money fields — scope note below) | ✅ done (`704cfc80`) | decoy-resumen (both profiles) calibrated (margin 0.45); 5-demo gate NON-VACUOUS (5/7 fields extract clean on real data, no flip); negative control flips CL-21 — VERIFIED |
| C1.5 | Architecture-enforcement / drift-guard test | ✅ done (`c8cb2ea2`) | coverage (scored field ↔ calibration ↔ specimen, walks `FieldCalibrationTable.Resumen.Keys` live) + clean-field Conf≥0.8 sweep — VERIFIED (434/532/178/158) |

## ✅ EPIC COMPLETE (2026-07-16) — all 7 stories done, verified from ground truth, committed on `Liv`
Everything ships **DARK** (`EmitGeometricConfidence` default false). Test floor: Extraction **434** · Validation **532** · Orchestration **178** · Application **158**, build 0/0. **OPEN owner-gated decision:** arming — one flag couples BOTH slices; RESUMEN arming is validated on real demo data (non-vacuous, no flip) but Tasa/Cat is synthetic-only. Options: keep dark / arm both (one flag) / split the flag to arm RESUMEN only. Commits local, not pushed.

## ⚠️ CRITICAL FINDING from C1.0a (gates C1.0b design) — SEPARABILITY
The `s-c1-swap` fixture as built = `"27.36% sin IVA 28.86%"` (numbers transposed, `sin IVA` kept BETWEEN the two `%` tokens — same structure as baseline `"28.86% sin IVA 27.36%"`). This **proves the confident-wrong VERDICT exists** (CL-10 flips), BUT the two layouts are **geometrically identical** — `sin IVA` is equidistant, token count is 2, columns align — so **NO geometric signal separates them.** The party's load-bearing signal (sibling `sin IVA`) can only catch a swap if the marker TRACKS the true CAT (i.e. a realistic swap displaces `sin IVA` off the mis-picked leftmost token). 
**⇒ C1.0b's FIRST job:** decide whether a *realistic* CAT/TASA swap displaces the `sin IVA` marker (per the party's signal intent — `sin IVA` legally qualifies CAT). If yes, build/calibrate a marker-displaced swap variant (e.g. `"27.36% 28.86% sin IVA"`) that is BOTH faithful to real Banamex layout AND geometrically separable (margin≥0.15). If the only realistic swap is geometrically invisible → **STOP + escalate to owner** (lever not viable via geometry alone). Do NOT shop for a passing fixture; justify layout from the realbanamex reference. Keep the current central-`sin IVA` `s-c1-swap` as a negative control ("geometrically-invisible swaps exist").
Deferred by C1.0a: `decoy-resumen-amount` (belongs in C1.4).

## Key file map
- Generator: `scripts/veriqan-corpus/synth_gen.py` → `Prisma/Fixtures/PRP2/synthetic/corpus-manifest.json`
- Extractor: `02 Infrastructure/Veriqan.Infrastructure.Extraction/PdfPigStatementFieldExtractor.cs` (`ExtractTasaAndCat` ~1265-1287, `ScanResumenColumn` ~1438-1512)
- Domain type: `01 Core/Veriqan.Domain/Extraction/ExtractedField.cs` (`.Found` hardcodes 1.0 @ :82)
- Calibration harness (C#): `08 Tests/03 Orchestration/Veriqan.Orchestration.Tests/Calibration/` (`CorpusSchema.cs`, `CalibrationDriverTests.cs`)
- Demo verdict E2E: `VecChecklistDemoE2ETests`

## Adversarial-review cadence
After C1.0a, after C1.0b (the gate itself is the review), after C1.3, after C1.5. Hunt: false-abstain (Lens C) + any verdict flipping the wrong way.

## Log
- 2026-07-16 — Tracker opened; tasks #1–#7 created. Scope confirmed = full C1 with C1.0b as hard internal gate. Starting C1.0a + C1.1 in parallel.
- 2026-07-16 — C1.1 done (`4af169e5`, bit-identical green). C1.0a done (`f37a60b2`, swap→confident-wrong CL-10 verified) + surfaced the SEPARABILITY finding. C1.0b spike = **GO**, margin 0.35, verified from ground truth (read prototype/harness, reran 14 tests). Make-or-break gate PASSED → proceeding to C1.2. Carry-forward: C1.3 negative control = `s-c1-swap-displaced`.
- 2026-07-16 — C1.2 done. Lifted the spike mechanism verbatim into production: `Confidence/GeometricPlausibilityScorer.cs` (new file, `02 Infrastructure/Veriqan.Infrastructure.Extraction/`) — `GeometricToken`/`GeometricSignals`/`FieldCalibration`/`FieldCalibrationTable.TasaCat` (SiblingAbsentPenalty=0.55, CompetitionExcessPenalty=0.65) + `GeometricPlausibilityScorer.Score`/`IsSiblingAdjacentToPick` (rank-based, matches C1.0b's perturbation-proofed version). Wired into `ExtractTasaAndCat` (now takes `emitGeometricConfidence`); score is ALWAYS computed, only the `ExtractedField.Found` overload choice is gated. Ship-dark switch = new `emitGeometricConfidence` ctor param on `PdfPigStatementFieldExtractor`, default `false` (mirrors the existing `enableCatalogImageHashing` opt-in pattern) — NOT tenant config, NOT flipped on. 28 new tests (12 unit `GeometricPlausibilityScorerTests` + 16 `GeometricPlausibilityCalibrationTests`, the latter running the REAL extractor end-to-end with the flag armed over the synthetic corpus — production-wiring analogue of the C1.0b spike, not a duplicated band-locator). Verdict: margin unchanged from spike (min clean 1.0 − max ambiguous 0.65 = 0.35 ≥ 0.15). Flag-off byte-identical proven by an explicit `[Theory]` asserting Confidence==1.0 on all 6 fixtures (clean AND ambiguous) with the flag off. All 4 gates green: Extraction 398 (370+28), Validation 532, Orchestration 165, Application 158 — unchanged where expected. Solution build 0/0. Next: C1.3 (arm via verdict-diff harness, negative control = `s-c1-swap-displaced`).
- 2026-07-16 — **C1.3 done — ARMED.** Built `GeometricConfidenceArmingGateE2ETests.cs` (new, `08 Tests/03 Orchestration/Veriqan.Orchestration.Tests/`): (A.1) a 5-demo `[Theory]` (`good.pdf`, `bad-math-cl21.pdf`, `bad-font-cl35.pdf`, `scanned.pdf`, `compliant-master.pdf`) that runs each fixture through the full `IVerificationPipeline` twice (flag off/on via a pinned `PdfPigStatementFieldExtractor` registration ahead of `AddVeriqanExtraction()`'s TryAdd) and asserts `Signal`/`FailCheckIds`/`InsufficientDataCheckIds`/Tasa+Cat `Confidence` are byte-identical; (A.2) a mandatory negative-control `[Fact]` on `s-c1-swap-displaced.pdf` proving CL-10 flips confident-Fail (flag off) → honest `InsufficientData` (flag on). Both green — **6/6 passed**. **IMPORTANT FINDING (verified via a throwaway diagnostic, then removed):** on all 5 demo fixtures, Tasa/Cat are `NotExtracted` (confidence 0.0 in both flag states) — `ExtractTasaAndCat` never matches this real-Banamex layout's rate line at all. So the 5-demo diff proves the flag changes nothing else on real-bank data, but the false-abstain-on-a-clean-pick claim is proven ONLY on the synthetic corpus (C1.0b/C1.2) + the `s-c1-swap-displaced` flip here — **not** on any live non-synthetic fixture. Documented prominently in the new file's class remarks. Per the DoD's literal STOP condition (any of the 5 demo verdicts differing) not being tripped, proceeded to arm. **Armed via**: `PdfExtractionOptions.EmitGeometricConfidence` (new bool property, default `true`) bound from config section `Veriqan:PdfExtraction` (env var `Veriqan__PdfExtraction__EmitGeometricConfidence` to kill); `VeriqanExtractionExtensions.AddVeriqanExtraction` (`02 Infrastructure/Veriqan.Infrastructure.Extraction/DependencyInjection/`) now registers `PdfPigStatementFieldExtractor` via an explicit factory reading that option instead of implicit DI-activation, so the single composition root (`VeriqanOrchestrationExtensions.AddVeriqan`, called by both Veriqan Web.UI and Veriqan Worker `Program.cs`) picks it up automatically. `PdfPigStatementFieldExtractor`'s own ctor default (`emitGeometricConfidence = false`) is UNTOUCHED — tests that construct it directly (all existing calibration/scorer tests) are unaffected; only DI-resolved construction changed. Re-ran all 4 suites post-arming: Extraction 398/398, Validation 532/532, Application 158/158, Orchestration 171/171 (165+6 new) — all green, no pre-existing verdict regressed (confirms `s-c1-swap.pdf`'s CL-10-Fail via `SyntheticDefectVerdictE2ETests`, which calls `AddVeriqanExtraction()` with no override and is now armed-by-default, still passes — the central-marker blind spot holds as documented). Build 0/0. `calibration-report.md` regenerated as a side effect of the Extraction run — reverted before commit. Next: C1.4 (extend to `ScanResumenColumn`, 11 money fields) — reuses this exact arming seam (`PdfExtractionOptions`/composition-root pattern).
- 2026-07-16 — **C1.4 done — SHIPPED DARK (owner instruction: do NOT auto-arm).** **Note found at C1.4 start:** `PdfExtractionOptions.EmitGeometricConfidence` is actually `false` by default in the current code (no C# field initializer) and its own doc comment says "ships DARK (owner ruling 2026-07-16)" — the C1.3 log entry above ("new bool property, default `true`") describes an intermediate state that was reverted by that same-day owner ruling; the tracker log was never updated to say so. Confirmed this is the TRUE current state before touching anything.
  **Scope finding (studied `ScanResumenColumn`/`ExtractResumenField`/`ExtractNivelDeUsoField` first, per instructions):** of the 11 RESUMEN/NIVEL/DESGLOSE fields, only **7** (`AdeudoPeriodoAnterior`, `CargosRegularesNoMeses`, `CargosComprasAMesesCapital`, `MontoIntereses`, `MontoComisiones`, `IvaInteresesYComisiones`, `PagosYAbonos`) flow through the dual left/right-column `ScanResumenColumn` mechanism the signal set targets. `SaldoCargosRegulares`/`SaldoCargosAMeses` (`ExtractNivelDeUsoField`) use a structurally different single-pass RIGHTMOST-in-band pick (no label-relative leftmost pick, no dual pass) — extending the *dual-pass* signal #3 there would be invented geometry, so honestly out of scope for C1.4's signal set.
**⚠️ CORRECTION (adversarial review 2026-07-16, finding #1 — the original C1.4 claim here was WRONG):** `TotalCargos`/`TotalAbonos` do NOT "come from a DESGLOSE-table sum with no geometry." They are extracted by `TryParseTotalRow` (`PdfPigStatementFieldExtractor.cs:~1943-1958`), a genuine **positional label→value pick** — it locates the "Total cargos/abonos" label band and returns the first amount-pattern token to its right via `ExtractedField.Found(parsed, locator)` — structurally the same class of pick as `ScanResumenColumn`, with the **same wrong-token mis-pick risk**. (The movements SUM the original note conflated is a separate cross-check computed by the *validation rule* `Cl44DesgloseTotalsMatchRule.cs:125-129`, not the field source.) ⇒ These 2 fields **DO have a scorable geometric signal that was NOT built** — a genuine (small) coverage gap, **DEFERRED, not impossible**. NOT a safety issue (both still report constant 1.0 regardless of the flag, unchanged from pre-C1). Owner-gated: build a C1.4 addendum (label-adjacency signal for the total-row pick) or accept the deferral.
  **Signal design (`Confidence/GeometricPlausibilityScorer.cs`, extended not replaced):** new `ResumenGeometricSignals(LabelRankAdjacent, DualPassDisagreement)` / `ResumenFieldCalibration(LabelNotAdjacentPenalty, DualPassDisagreementPenalty, MaxLabelToPickRankGap)` / `ScoreResumen` / `IsValueRankAdjacentToLabel` — same MULTIPLICATIVE shape as C1.2, deliberately separate types (Tasa/Cat path untouched). Signal #1 = is the picked amount within `MaxLabelToPickRankGap` ordinal ranks of the label's own LAST token (measured from the label's END, not start, so — unlike the CUT signal #4 — it does NOT vary with label word-count). Signal #3 = binary dual-column-pass disagreement (`ExtractResumenField` now always runs BOTH the right- and left-column `ScanResumenColumn` passes, even once a winner is known, purely to compute this diagnostic — the winning-value precedence is unchanged, proven byte-identical: flag-off suite reran green with the SAME extracted values).
  **`MaxLabelToPickRankGap` calibration was NOT guessed — measured against real code twice, after two real bugs:** (1) first attempt used a rank gap threshold of 2, assuming every `labelTokens` array spans the full printed label; a PdfPig word-dump diagnostic (throwaway, removed) showed `CargosRegularesNoMeses`'s labelTokens `["Cargos","regulares","(no"]` matches only a PREFIX of "Cargos regulares (no a meses)", leaving 2 real trailing words before the sign+amount — a genuine clean-pick rank gap of 4 (same for `IvaInteresesYComisiones`). Raised the threshold to 4 (documented in `FieldCalibrationTable.ResumenDefault`'s remarks). (2) First decoy-specimen attempt used 2 junk tokens ("Nota","1") placed too close together; PdfPig's OWN word-tokenizer silently MERGED them into one word ("Nota1") — verified via the same diagnostic — collapsing the intended rank gap from 3 to 2 (below even the corrected threshold). Redesigned with 4 single-digit junk tokens, each empirically spaced ≥2.3pt (above the corpus's own measured 2.22pt "sin"/"IVA" safe-gap) to guarantee separate PdfPig words — rank gap 5, now clears the threshold.
  **Generator (`scripts/veriqan-corpus/synth_gen.py`):** `decoy-resumen-amount` (dummievec, right-column pass) + `decoy-resumen-amount-realbanamex` (real-Banamex, left-column pass — Mary's non-negotiable, a genuinely different `labelMinX`/`labelMaxX`/`amtMaxX` code path) — both mis-pick `AdeudoPeriodoAnterior` to an in-range decoy amount (real number, wrong slot) while the other 6 RESUMEN fields stay untouched clean picks; `geometryDefect`/`confidenceExpectations` fields match the C1.0a contract. Registered in `CORPUS_SPECIMENS`, `--write-index` regenerated (19 specimens). **Process note:** an initial `--profile all` run byte-churned 17 unrelated pre-existing committed fixtures (non-deterministic PDF bytes on regen, same size — likely embedded timestamp metadata PyMuPDF adds by default, a pre-existing generator gap, not investigated further as out of C1.4 scope) plus 3 manifests that had genuinely drifted from the current generator source (stale commits, unrelated pre-existing issue); reverted all 20 to `git HEAD` and used `--write-index` (index-only, no PDF/manifest regen) to get a clean, purely-additive `corpus-manifest.json` diff instead.
  **Calibration** (`GeometricPlausibilityResumenCalibrationTests.cs`, new, Extraction.Tests, mirrors `GeometricPlausibilityCalibrationTests`): flag-off byte-identical (4 fixtures × 7 fields); flag-on clean baselines (both profiles) all 7 fields at ceiling 1.0; flag-on decoy fixtures — `AdeudoPeriodoAnterior` < 0.8, the other 6 fields stay 1.0; verdict `[Fact]` — **min(clean)=1.0, max(decoy)=0.55, margin=0.45** (well above the 0.15 bar, no threshold-shopping needed — STOP condition never approached). The 2 new specimens are ALSO picked up automatically by the pre-existing index-driven `SyntheticGoldenRoundTripTests`, independently confirming the extractor's actual mis-pick matches the manifest's hand-predicted decoy value.
  **Verdict-diff harness** (`GeometricConfidenceResumenArmingGateE2ETests.cs`, new, Orchestration.Tests, mirrors C1.3's file): 5-demo `[Theory]` + `decoy-resumen-amount` CL-21 negative-control `[Fact]`, both green. **THE C1.4 FINDING (non-vacuous, unlike C1.3's Tasa/Cat gate):** a throwaway diagnostic (removed) showed 5 of 7 RESUMEN fields DO extract on 4 of the 5 real demo fixtures (`good.pdf`/`bad-math-cl21.pdf`/`bad-font-cl35.pdf`/`compliant-master.pdf`) — `CargosRegularesNoMeses`/`CargosComprasAMesesCapital`/`MontoIntereses`/`MontoComisiones`/`IvaInteresesYComisiones`, all real, non-trivial values (e.g. `CargosRegularesNoMeses`=6479.43). **NO verdict or confidence changed off→on on any of the 5 demo fixtures** — every extracted field scored the ceiling 1.0 when armed, confirming the rank-adjacency signal does NOT under-rate a real clean pick on this bank's actual layout (the cardinal C1 false-abstain risk did NOT materialize). `AdeudoPeriodoAnterior`/`PagosYAbonos` are `NotExtracted` on all 5 demo fixtures (this real layout doesn't match those two label patterns) and `scanned.pdf` extracts nothing (no text layer) — unaffected either way. Negative control: `decoy-resumen-amount.pdf` flips CL-21 (which consumes `AdeudoPeriodoAnterior`, see `Cl21PagoParaNoGenerarInteresesRule`) from a confident (wrong-operand) verdict flag-off to an honest `InsufficientData` abstain flag-on; the other 6 fields on that same fixture stay at ceiling — proven un-collateral.
  **Housekeeping fix (unrelated pre-existing bug, low-risk, directly relevant to this story's "confirm shipped dark" claim):** `VeriqanExtractionExtensions.AddVeriqanExtraction`'s comment claimed `EmitGeometricConfidence`'s default is `true` — stale (the property has no initializer; C# default is `false`, matching `PdfExtractionOptions.cs`'s own doc comment and the actual runtime behaviour observed in every test run this session). Fixed both comments; explained the ONE-flag-arms-both-slices design and the C1.4 non-vacuous-but-still-owner-gated stance.
  **`AntiFalseConfidenceHonestyTests.DeterministicCorpus_IsNonEmpty_AndExcludesExactlyTheKnownOcrSpecimens`** needed updating (index-driven, hardcoded counts): total 17→19, deterministic 14→15, and `decoy-resumen-amount-realbanamex` added to `NativeOcrExcludedSpecimenIds` (reuses `PAGE1_TOKENS_S622`, which — like `s622-realbanamex-baseline` — never prints the product name positionally, so it escalates to the real OCR rung exactly like that baseline); `decoy-resumen-amount` (dummievec) resolves Product positionally like every other `s6211-*` specimen and is NOT excluded.
  **Verified counts (rebuilt DLLs, bare `net10.0/`):** Extraction **420** (398+22) · Validation **532** (unchanged) · Application **158** (unchanged) · Orchestration **178** (171+6 new +1 net from other index-driven theories auto-picking up the 2 new specimens) — all green. Solution build 0/0 (full `dotnet build` of the `.sln`). `calibration-report.md` regenerated as an Extraction-run side effect — reverted both times before finishing.
  **Shipped DARK, confirmed**: `PdfExtractionOptions.EmitGeometricConfidence` default is `false`; no config/DI default was flipped. Arming (for either slice) remains an explicit owner decision — the C1.4 evidence (non-vacuous, no flip on 5/5 real demo fixtures) is handed to the owner alongside C1.3's, not auto-applied.
  Next: C1.5 (architecture-enforcement test — every scored field ↔ calibration entry ↔ specimen, `Confidence≥0.8` sweep over every clean field in every specimen).
