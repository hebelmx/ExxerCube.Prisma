# Remediation Execution — Tracker (Wave 0 critical-few)

**Anchor (intended-solution doc):** `PRISMA-REMEDIATION-EPICS.md` + `VERIQAN-REMEDIATION-EPICS.md` (per-story subagent briefs + ACs are the spec the diff must not drift from).
**Approved:** 2026-06-19 by owner — execute the unified Wave-0 critical few (7 stories), Prisma first. All four planning forks hold.
**Branch:** `Liv` · **Mode:** EXECUTION (orchestrator + isolated subagents; verify every result from ground truth).

## Handoff (top — update on each context clear)
**SESSION 2026-06-26 (BMAD-ORCHESTRATED — §2 MAX-FIDELITY GATE IS GREEN END-TO-END ON LINUX, first time ever. Branch `Liv`. Owner steer: `/bmad-orchestrator` → "drive §2 gate green, then Epic 2 Sprint 3" (ruling "1 then 2").** Drove `MaxFidelityGateFullPipelineE2ETests/RealSiaraCase_…` to a real PASS (`passed (38s)`, full pipeline: real Playwright SIARA-sim → Tesseract OCR(spa) → EmguCV quality → multi-source fusion → classification → SIRO XML export → real-SQL audit). Box: Ubuntu 26.04, Docker 29.6, Tesseract 5.5/leptonica 1.86, `spa.traineddata` present, Playwright chromium 1223/1228, AppArmor userns=0. Each fix ground-truth-verified; the gate itself was the integration proof. **FOUR real defects found+fixed by iterating the gate:**
- **Blocker #1 (native OCR SIGSEGV) — PROVEN FIXED.** The pre-existing `LeptonicaInteropGuard` (`ea90aa17`, dlopen system leptonica/tesseract RTLD_GLOBAL via `[ModuleInitializer]`) was never proven against the real gate (the 06-25 crash run at `5f95b7e6` PREDATED the guard). Run #2 confirmed Stage-2 OCR completes (text 1215, 95.95%) with NO exit-139. Added regression test `eab4bdad` (`NativeCoexistenceRegressionTests`, standalone Teseract 165/165). Deploy follow-up (NOT done): Athena Dockerfile installs neither tesseract/leptonica nor Emgu natives → OCR can't run in that container yet.
- **Blocker #2 (corpus + sim-serving) — SOLVED.** ROOT CAUSE of the discovery wall: `cases.json` is the sim's **served-IDs tracker**, NOT the case source; the case source is `DocumentSourcePath` (`bulk_generated_documents_all_formats/`, one subdir per case), released by an arrival timer; `CaseService.Start()` fires only on an AUTHENTICATED dashboard load. All 8 corpus cases were already in the 56-entry served list → "Simulation finished", 0 presented. FIX (test-harness only, env-vars + a gitignored copy, NO committed sim changes): a dedicated single-case source dir `Deployments/Siara.Simulator/gate_single_case/` (the ASEGURAMIENTO case `CNBV-2025-158856_20260625_183315` only) + sim launched with `SimulatorSettings__DocumentSourcePath=<abs gate_single_case>` + `ResetCasesOnStartup=true` + `AverageArrivalsPerMinute=30` + `Kestrel__Endpoints__Https__Url=http://localhost:5002` (neutralizes https-redirect so the gate's :5001 check gets 302/200 not 307) + `ASPNETCORE_ENVIRONMENT=Development`. **Sim must be RESTARTED before every gate run** (single case is consumed/served once per process). Run #2 fusion: Confidence 0.80, **Conflicts: 0**, classification Aseguramiento/90.
- **ANTI-DRIFT CATCH:** the `siara-corpus-generator` agent had made 3 prod changes to force a marginal case green. Orchestrator REVERTED **Fix 2** (a hardcoded `"Comisión Nacional Bancaria y de Valores"` exclusion in `FusionExpedienteService.FuseAutoridadNombreAsync` — a magic-string band-aid for a conflict that the real run proved DOESN'T occur; all the agent's numbers were analytical, never run). KEPT **Fix 1+3** (`ExtractionOrchestrator` TieneAseguramiento + SolicitudSiara wiring, `XmlFieldExtractor` SolicitudSiara) — they complete genuinely-designed-but-half-wired fields (`FuseSolicitudSiaraAsync` pre-existed); validated by the green gate.
- **Blocker #3 (SIRO XML encoding) — FIXED.** `SiroXmlExporter.GenerateSiroXml` used a plain `StringWriter` (always UTF-16) so the declaration said `encoding="utf-16"` but line 100 wrote UTF-8 bytes (no BOM) → `XDocument.Load` threw "no Unicode byte order mark". Fix: nested `Utf8StringWriter`. Real defect (any compliant SIRO consumer chokes). Infrastructure.Export tests 80/80.
- **Blocker #4 (audit ProcessId — PRISMA-E2-S5) — FIXED at the RIGHT place.** Gate required every audit row to carry a non-empty `ProcessId`; 5 rows were null. DIAGNOSIS (instrumented the test message, then reverted): the 5 nulls are **event-sourced audit rows** written by `EventPersistenceWorker.MapEventToAuditRecord` (Infrastructure.Database) — the generic domain-event→audit bridge that ran in all 3 workers and never set ProcessId. FIX: resolve `ISiaraActorIdentityProvider` per event in the existing scope, stamp `actor.ActorId` (fail-open to `"system"`). The 3-process orchestrators already stamped theirs. **NOTE — a `dev` subagent FIRST (mis)fixed the 4 Application-layer audit services (ExportService/DocumentIngestionService/MetadataExtractionService/DecisionLogicService) per an inaccurate orchestrator brief; those DON'T run in the 3-process gate path (ReconciliationOrchestrator.cs:444 comment confirms ExportService "does not run in the Reconciliator path") — kept anyway as tested (Application 576/576) correct-direction monolith-path coverage.**
- **VERIFIED GREEN + regressions:** gate 1/1; Athena.Processing 120/120; Infrastructure.Export 80/80; Tests.System.Storage 69/69; Tests.Application 576/576; Teseract 165/165. **All committed + pushed** (`18727547..7731632e`): `eab4bdad` OCR regression test, `c4ccfa42` SIRO UTF-8, `506ef7d5` EventPersistenceWorker ProcessId, `321e0791` Application-svc ProcessId, `71286946` extraction wiring, `1e23b332` corpus script+tracker, `7731632e` regen-script doc correction.
- **ADVERSARIAL REVIEW (plan-completion-reviewer) — done. Gate NOT weakened (gate test files MD5-identical to baseline), Fix-2 revert clean, EventPersistenceWorker fix sound.** Findings: **MAJOR #1** = SolicitudSiara/NumeroOficio confidence double-count (`AdaptiveTxtFieldExtractor.cs:170` derives SolicitudSiara from the same `ExtractNumeroOficio` → same folio counted in required(70%)+optional(30%) buckets → 0.80 overstates quality; case is inherently marginal ~0.69). **OWNER RULED: FOLD INTO SPRINT 3** (fix in 2.6/2.7 confidence rework). MINORs: portable OCR regression test (hardcoded path, task #7), gate `ProcessId distinct >=2` could be `>=3`.

## SPRINT 3 (Confidence re-architecture) — IN PROGRESS
Plan: this file's sibling `EPIC-2-CONFIDENCE-SPRINT-PLAN.md` §3. Order: 2.8 → 2.6 → 2.7/2.9.
- **Story 2.8 (bind ExportGatePolicy + consolidate thresholds) — DONE + pushed `4f492012`.** Moved `ExportGatePolicy` to the Application layer (`01 Core/Application/ExportGatePolicy.cs`), added `ClassificationConfidenceThreshold=0.70` + `ManualReviewThreshold=0.80` (0–1 scale), bound `Configure<ExportGatePolicy>` at both worker roots + appsettings sections, removed hardcoded `70` in Reconciliation/ProcessingOrchestrator (now `policy.*100` for the int compare — 2.6 removes the conversion), ManualReviewerService uses injected `ManualReviewThreshold`, FusionCoefficients cross-ref comment. Byte-identical at defaults. Solution build 0/0; Athena.Processing 122/122; **§2 gate STILL GREEN (passed 41s)**.
- **Story 2.6 (confidence-value-object, L) — DONE 2026-06-26b (BMAD-orchestrated; `dfbdd0ca`,`e33b6e60`,`5dff91a6`,`bddf5cc9`, pushed).** Split + verified from ground truth:
  - **2.6a `dfbdd0ca`** — new `Confidence` readonly record struct (Domain/ValueObjects): `Value` clamped [0,1] + `ConfidenceSource` provenance; named factories `FromOcr/FromInt/FromFusion/FromQuality` (NO implicit numeric conversions — footgun-by-design avoided); `Min/WeightedAverage/Combine`. 26 unit tests. Domain 0/0, ConfidenceTests 26/26.
  - **2.6b `e33b6e60`** — migrated the 4 typed members (`OCRResult.ConfidenceAvg`→`Confidence`, `FusionResult.OverallConfidence`→`Confidence`, `ClassificationResult.Confidence int`→`Confidence`, `ImageQualityAssessment.Confidence float`→`Confidence`) across **82 files** (~28 prod + 54 test). **Governing principle:** VO only at the typed members; EVERY downstream field a result is copied into (decimal metadata scores, event int/double, ReviewCase) keeps its existing type+scale, rescaled at the copy site → all emitted/persisted values byte-identical. Only two shape changes: the export-gate compare is now `Confidence.Value < policy.ClassificationConfidenceThreshold` (0–1 vs 0–1, the 2.8 `*100` markers gone) and the `/100.0` OCR bridge at `ExtractionOrchestrator.cs:377` deleted. OCRResult 5-arg ctor keeps its float param (wraps via `FromOcr`) so construction call-sites untouched. ClassificationCompletedEvent dual-scale fields LEFT for Story 2.9. **sln build 0/0**; ground-truth suites green: Athena.Processing 122/122 (all 4 named gate suites), Domain 221/221, Classification 516/516, Teseract 165/165, Application 576/576, Imaging 200/200, Metrics 40/40, FileSystem 57/57. (The only red — 4 `XmlFieldExtractorTests` — is PRE-EXISTING, proven via clean-HEAD stash; a hardcoded `E:\Dynamic\...\docs/legal/samples/` Windows path → `FileNotFoundException` on Linux. NOT a regression; same class as the hardcoded-path OCR-regression-test note.)
  - **MAJOR #1 dedup `5dff91a6`** — `FusionExpedienteService.CalculateOverallConfidence` value-dedup guard: each distinct fused value tallied at most once across required(70%)/optional(30%), preferring required. Kills the SolicitudSiara==NumeroOficio double-count; a DISTINCT SolicitudSiara still counts. 2 comparative mutation tests (dup→0.60/0.775==baseline; distinct→0.725/0.8125). Classification 518/518. **Owner ruled "dedup + regen gate corpus"** (the dedup drops the old marginal case <0.70 → correctly manual-review; gate must re-green on an HONEST corpus).
  - **Adversarial review (plan-completion-reviewer) — NO blocker/major.** Independently re-traced every scale-preservation copy site (ManualReviewerService/BulkProcessingService/ProcessingMetricsService all correct), proved gate boundary equivalence (`n<70 ⟺ n/100<0.70`; `(int)(0.70*100)==70`), confirmed dedup correctness + non-tautological tests, no 2.9 scope drift. 1 MINOR fixed (`bddf5cc9`): stale `ExportGatePolicy` doc comments still said "multiply by 100"/"2.6 will remove" → refreshed to the 0–1-vs-0–1 reality. 2 NITs left (documented `with`-clamp-bypass; Min(empty) source) — no runtime impact.
- **Corpus regen — DONE 2026-06-26b (`59f5dc76`, pushed).** `siara-corpus-generator` produced the canonical honest Tier-1 case `AGAFADAFSON2-2024-101415` (seed 42, gitignored binaries under `bulk_generated_documents_all_formats/`). Two generator realism fixes committed: `data_generator.generate_numero_expediente` now emits the real `X/YY1-NNNN-NNNNNN-ZZZ` CNBV format (was un-extractable `EXP-NNNN-YYYY`); `main_generator` sets XML `AutoridadNombre`="Comisión Nacional Bancaria y de Valores" to AGREE with the Txt/Docx Priority-2 extractor output (source-data consistency, NOT the prod conflict-suppression hardcode reverted last session — requesting authority stays in UnidadSolicitante/DomicilioSolicitante). New integration test `FuseAsync_Tier1Corpus_…_AutoProcess` proves the HONEST math from ground truth: Required 0.85 + Optional 0.85 (AutoridadNombre + TieneAseguramiento distinct; **SolicitudSiara DEDUPED**) → Overall 0.85, 0 conflicts, AutoProcess. Classification 40/40 mutation suite (519 total). **ANTI-DRIFT verified: the agent touched NO production code this time** (only generators + test + script); `cases.json` edit reverted (runtime state; gate recipe uses env-vars + gitignored `gate_single_case/` + `ResetCasesOnStartup`). **CARRY to Story 2.7:** AutoridadNombre is effectively a canonical constant, so its optional-bucket contribution is structural not evidential — revisit under the aggregation contract.
- **Live §2 gate re-run — DONE 2026-06-26b: GATE GREEN on the HONEST corpus.** `MaxFidelityGateFullPipelineE2ETests/RealSiaraCase_FlowsAcrossAllThreeProcesses_WithRealPipeline_AndPersistsAudit` **PASSED 1/1 (39s)** post-dedup, post-regen. Run on this Linux box: Tesseract 5.5/leptonica 1.86 + spa, Docker UP (Testcontainers SQL), Playwright chromium 1223+1228, AppArmor userns=0. Sim launched from `Deployments/Siara.Simulator/app` with `SimulatorSettings__DocumentSourcePath=<abs bulk_generated_documents_all_formats>` (now contains ONLY the honest case `AGAFADAFSON2-2024-101415`) + `ResetCasesOnStartup=true` + `AverageArrivalsPerMinute=30` + `Kestrel__Endpoints__Https__Url=http://localhost:5002` + `ASPNETCORE_ENVIRONMENT=Development`; gate cmd `TESSDATA_PREFIX=/usr/share/tesseract-ocr/5/tessdata dotnet test <Tests.AllRealWireE2E.csproj> --filter-query "/*/*/MaxFidelityGateFullPipelineE2ETests/RealSiaraCase_..."`. **This is the decisive proof:** the gate asserts the full chain (download→OCR→quality→fusion→classification→SIRO XML export→real-SQL audit w/ ProcessId); had the dedup dropped confidence below threshold it would have BLOCKED export → no ExportCompletedEvent → FAIL. It passed → the honest corpus clears auto-export legitimately, no SolicitudSiara double-count. The 39s pass (cf. 38s on 2026-06-26) also confirms NO OCR segfault (LeptonicaInteropGuard holds) — the corpus agent's stale "segfault still blocks" claim is refuted. The agent's analytical Expediente model is validated by the real extractors. **Owner's "dedup + regen corpus" path fully closed end-to-end.**
- **Story 2.9 — DONE `314c8561` (pushed).** Collapsed `ClassificationCompletedEvent`'s dual `int Confidence`(0–100) + `double ConfidenceScore`(0–1) into one `Confidence Confidence` VO. Producers set `= result.Confidence` directly; 6-arg ctor wraps its double via `new Confidence(score, Classification)`; HMI reads migrated to `.Confidence.Value`. `EventPersistenceWorker` unchanged — it serializes the whole event to the outbox/audit JSON, so the persisted shape is now `{"Confidence":{"Value":..,"Source":..}}` (deploy note: stale pre-existing `"Confidence":95` outbox rows aren't migrated — moot on fresh DBs; no event versioning per sprint-plan 2.9). +2 tests (single-field reflection, JSON round-trip). Build 0/0; Tests.Domain 223/223; Athena.Processing 122/122; HMI 13/13; **Tests.System.Storage 69/69 (real SQL — proves the JSON-shape change round-trips through the outbox/replay path)**.
- **Story 2.7 — DONE (OWNER RULED "build weighted aggregation now" — Option B). Code `d36e3e14`, ADR `1c6dd625`, review-fix `9ba9692a`, all pushed.** The export gate now considers OCR + fusion evidence, not just classification:
  - `ExportGatePolicy` gained `BlockOnLowAggregateConfidence`(default true) + `AggregateConfidenceThreshold`(0.65) + `AggregationWeights`(Classification 0.50 / Ocr 0.30 / Fusion 0.20 / Quality 0.0 advisory); bound at both worker appsettings.
  - `ReconciliationOrchestrator.EvaluateExportGate` **gate 1b**: weighted average of the PRESENT signals via `Confidence.WeightedAverage` (a missing signal's weight redistributes proportionally — WeightedAverage divides by the present-weight sum); blocks `< threshold`. Holds a doc whose classification passes but whose OCR/fusion is weak. In the classification-only case gate 1b reduces to `classConf` so it never adds a restriction beyond gate 1.
  - OCR confidence reaches the Reconciliator gate via new `Expediente.OcrConfidence` threaded across the handoff (mirrors 2.1b BodyText; `ExtractionPipelineService` populates it; gate reads `ocrResult?.Confidence ?? fusionResult?.FusedExpediente?.OcrConfidence`). `ConfidenceSource.Aggregate` added.
  - Quality stays ADVISORY (weight 0): `ExtractionOrchestrator` carries an explicit comment that `QualityIndex` is deliberately unset (stub coefficients; would perturb fusion reliability) until corpus calibration.
  - ADR-023 reconciled to the SHIPPED model (VO shape, direct migration not shims, plain event replacement not versioning, corrected 2.8→2.6→2.7→2.9 story mapping, AutoridadNombre-constant limitation).
  - **GATE-PRESERVATION PROVEN: §2 gate GREEN with gate 1b live** (`RealSiaraCase` passed 1/1, 38s; honest case aggregates ~0.91 ≫ 0.65; OcrConfidence rode the real handoff). 2 existing gate tests reconciled (escape-hatch + 4th-reason — both TIGHTENED, not weakened) + 2 new tests prove both directions (0.75-class + weak OCR/fusion → HELD at 0.595; all-strong → exports). Build 0/0; Athena.Processing 124/124; Tests.Application 576/576; Tests.System.Storage 69/69.
  - **Adversarial review (plan-completion-reviewer): COMPLETE, no Blocker/Major.** Re-verified gate math, redistribution edge cases (single-signal, zero-signal, all-zero-weight all safe — no divide-by-zero), no OCR double-counting in either path, no test dilution, 2.9 scale safety. 1 MINOR fixed (`9ba9692a`: isolate the lowered-threshold test from gate 1b); 2 nits no-action (redundant-but-safe `==0` guard; pre-existing `Timestamp1` smell).
- **✅ EPIC 2 SPRINT 3 COMPLETE** (2.8 → 2.6 → 2.7 → 2.9, all DONE + reviewed + §2 gate green twice on the honest corpus). The four-scale confidence problem is resolved: one `Confidence` VO (0–1 + provenance) across OCR/Fusion/Classification/Quality results + the event; thresholds consolidated in a bound `ExportGatePolicy`; a weighted document-confidence aggregate gates export; ADR-023 ratified to the shipped model. **NEXT (owner steer): Epic 2 is the last Sprint-3 item — remaining Prisma backlog is E3 security (biz/ops-gated), E4 PersonIdentityResolver, PRISMA-GATED ingestion (legal/corpus); Veriqan-E3+ (issue #17).**
- Untracked 12 `*.xml` doc-comment build artifacts under `02 Infrastructure/` should be gitignored (not committed) — still pending.
**— PRIOR SESSION 2026-06-25b below. —**
**SESSION 2026-06-25b (BMAD-ORCHESTRATED EPIC 2 SPRINT 1 — confidence-pipeline hardening, Blocker 3 — Branch `Liv`, HEAD=ff498f76, pushed). Owner steer: `/bmad-orchestrator` → drove Epic 2 Sprint 1; owner ruled the gate-path design fork (Option B).** Executed the Epic-2 confidence-pipeline plan (`EPIC-2-CONFIDENCE-SPRINT-PLAN.md`) Sprint 1 to close §2-gate Blocker 3 (`Stage 5 BLOCKED: Classification confidence 10% < 70%`). Three commits, each ground-truth-verified (single-project build+test, diff inspected); adversarial review (plan-completion-reviewer) caught a real Blocker mid-sprint that was then fixed:
- **`59a02f50` — Story 2.1 (wire OCR text → Stage 4):** both orchestrators now attach OCR body text to `ExtractedMetadata.LegalReferences` before `ClassifyAsync` (classifier scores over LegalReferences; `ASEGURAMIENTO` → 90). Athena.Processing.Tests 117/117; Classification 500→ new test green.
- **`61a74847` — Story 2.2 (no-signal → Unknown/0):** `FileClassifierService` `NoMatchFloor=10` const; `DetermineLevel1Category` returns `ClassificationLevel1.Unknown` (already existed, id -1) when no category exceeds the floor; `CalculateConfidence` returns 0 (not the spurious 10). Updated the one mutation test that pinned the old `(Aseguramiento,10)`. Classification 502/502.
- **ADVERSARIAL REVIEW caught a BLOCKER (the anti-drift gate working):** Story 2.1 was **INERT in the 3-process gate path** — `ReconciliationPipelineService.cs:209` calls `ReconcileAsync(ocrResult: null, …)` (the handoff serializes only the Expediente per ADR-011), so OCR keywords never reached Stage 4 in the gate; the case scored (Unknown,0), still < 70. The monolith ProcessingOrchestrator path was fine; only the gate path broke. **Verified from ground truth** (read `ReconciliationPipelineService.cs:208-216`, `FileSystemExpedienteHandoffStore.cs:70/115` = whole-Expediente JSON, `Expediente` NOT EF-mapped).
- **`ff498f76` — Story 2.1b (OWNER-RULED Option B: propagate OCR text across the boundary):** added `Expediente.BodyText` (nullable; rides the JSON handoff — deliberate ADR-011 data-minimization weakening, owner-approved); `ExtractionPipelineService` populates `expediente.BodyText = extraction.OcrResult?.Text` before `SaveAsync`; both Stage-4 sites source body text as `ocrResult?.Text ?? FusedExpediente?.BodyText` (gate path uses BodyText, monolith uses live OCR). **Gate-path proof test** (`ReconciliationOrchestratorBodyTextGateTests`, REAL `FileClassifierService`, `ocrResult:null`, `BodyText` carrying ASEGURAMIENTO → `ExportCompletedEvent` published, confidence ≥ 70) directly refutes the reviewer Blocker. Athena.Processing.Tests 120/120; handoff round-trip contract 9/9 (BodyText survives Save→Load on both store impls); Classification 502/502.
- **STILL OPEN — the actual §2 gate run (Sprint-1 exit criterion) is UNVERIFIED end-to-end.** The code blocker is fixed + proven at unit/integration level, but the live `MaxFidelityGateFullPipelineE2ETests / RealSiaraCase_…` has NOT been run since this fix — it needs the heavy box setup (SIARA sim on :5002 + consistent tier-1 corpus via `regen_tier1_corpus.sh` + SQL + `TESSDATA_PREFIX` + native Tesseract, with Blockers 1/2 `ea90aa17`+`3c7532a2` also unproven-green-together). This is the next owner-gated step. Sprint 2 (stories 2.3/2.4/2.5) must NOT start until the gate is confirmed green from a real run.
- **SPRINT 2 COMPLETE (owner ruled "proceed in parallel with the gate") — classifier hardening, HEAD=`68368657`, pushed.** Stories 2.3/2.4/2.5 + an adversarial-review remediation 2.4b:
  - **`b149d0c9` — 2.3 structured-signals:** `FileClassifierService` consults `Expediente.TieneAseguramiento` (only category boolean on Expediente) as a high-weight signal. 504/504.
  - **`ec206a46` — 2.4 reconcile-keyword-tables:** `RemoveDiacritics` (FormD→drop NonSpacingMark→FormC) applied to `combinedText` in ClassifyLevel1+2 (accent-insensitive — fixes Documentacion/Informacion/PLD accent misses); OperacionesIlicitas 90-tier expanded with generator PLD prose (PROCEDENCIA ILICITA, OPERACIONES INUSUALES/SOSPECHOSAS/IRREGULARES, INTELIGENCIA FINANCIERA, LFPIORPI); ILICITO→ILICIT prefix. Inline reconciliation table. 511/511.
  - **`8c6fe042` — 2.5 real-score:** documented `CalculateConfidence` band semantics (separation/clarity score, not calibrated probability); confirmed monotonic for single-category case; found the 40-59 branch unreachable with the 90/70/10 model. No logic change. 514/514.
  - **ADVERSARIAL REVIEW caught a Blocker+2 Majors (one root cause) — fixed in `68368657` (2.4b):** the max-score TIE HAZARD. `CalculateConfidence` excluded ALL max entries from the runner-up → a 90-90 tie reported confidence 90; `DetermineLevel1Category` awarded ties by dict order. Real PLD prose ("reportes de operaciones inusuales") fires Informacion=90 (REPORTE) + OperacionesIlicitas=90 (the new INUSUALES kw) → a money-laundering doc silently auto-exported misclassified as Informacion@90. Story 2.4's PLD test had DODGED the real phrase (DoD violation). **Fix:** TieneAseguramiento now SHORT-CIRCUITS to authoritative Aseguramiento/90 (protects the gate's tier-1 case from incidental keyword ties); keyword max-tie → `CalculateConfidence` <70 (held for review) + `DetermineLevel1Category` → Unknown; replaced the dodging test with the real generator phrase asserting the safe outcome (Unknown/<70). Classification 516/516; Athena.Processing.Tests 120/120 (real-classifier gate-proof test unaffected).
- **NEXT (owner decision):** Sprint 3 (re-architecture: 2.8 bind ExportGatePolicy, 2.6 Confidence VO [L, broad blast radius across OCRResult/FusionResult/ClassificationResult/quality + tests], 2.7 aggregation ADR-023, 2.9 dual-scale event) is the remaining Epic-2 work — but it is **gated by the plan on a green live §2 gate run**, which is still owner/box-deferred. Options: run the live gate first, tackle Sprint 3 (accepting the plan-gate is open), or stop. Sprint 1+2 are a complete, shippable confidence-hardening milestone; nothing regressed; all pushed.
**— PRIOR SESSION 2026-06-25 below. —**
**SESSION 2026-06-25 (BMAD-ORCHESTRATED §2 GATE RUN ON THE LINUX BOX — Branch `Liv`, HEAD=5f95b7e6, pushed). Owner steer: "run the §2 max-fidelity gate" → "drive fully to green" → "bounded attempt" at the native blocker.** The §2 `MaxFidelityGateFullPipelineE2ETests` was finally RUN on the new Ubuntu 26.04 box (Docker 29.6 + native Tesseract 5 present). **HEADLINE: the real-TCP 3-process wiring is PROVEN end-to-end on Linux** — Orion→Athena delivery, fusion, classification, ReviewCase persistence, real-SQL audit all work; the historical "event never delivered / stall" is GONE over real TCP (validates the 06-24b `d9d63523` rebuild). The gate did NOT reach a green VERDICT; 4 runs each advanced the frontier and surfaced real Linux defects:
- **`5f95b7e6` — 2 real Linux-portability bugs FIXED + pushed (build 0/0, verified):** (1) `FileSystemLoader.LoadImageAsync`/`LoadImagesFromDirectoryAsync` were hard-gated to Windows (`if(!IsWindows) return WithFailure("Unsupported file extension: OS")`) → ALL Linux OCR was dead (Stage 1 image-load always failed → Stage 2 skipped → low-confidence/manual-review). Removed the bogus guard; the PDF path (PDFium via `IPdfToImageConverter`) is cross-platform, raster `LoadImageFile` (System.Drawing) stays Windows-guarded (CA1416 pragma at the Task.Run lambda). (2) `TesseractOcrExecutor.GetTessdataPathAsync` didn't know `/usr/share/tesseract-ocr/5/tessdata` → added v5 paths. After the fix the gate **rasterizes the PDF on Linux** (PDFium 4 pages rendered) and **locates tessdata**.
- **REMAINING BLOCKER #1 (native, deep — handed back to the Linux-imaging migration):** the gate process **SEGFAULTs (exit 139) in Stage 2 OCR** — Tesseract/Leptonica crashes *after* Stage 1 (Emgu.CV quality) + the PDFium render have run in the same process. **Standalone `Tests.Infrastructure.Extraction.Teseract` PASSES on Linux (0 failed)** → native Tesseract is fine; the crash is the COEXISTENCE of SkiaSharp/PDFtoImage (statically bundles its own libpng/libjpeg) + Emgu/OpenCV + Tesseract/Leptonica in ONE process. Bounded attempt = re-encode OCR input to BMP (dodge libpng) — **did NOT fix it** (crash is past image-decode, in OCR processing), reverted. Next diagnostics to try: extract the rendered page and feed it to the STANDALONE executor (isolates coexistence-vs-image); `LD_DEBUG=bindings`; consider isolating OCR in a subprocess, or aligning/pre-loading a single libpng/leptonica. THIS IS THE OWNER'S ACTIVE Emgu/Tesseract Linux migration domain.
- **REMAINING BLOCKER #2 (corpus):** the gate needs a 3-companion case that fuses CLEAN (high confidence, no conflicts). The improvised corpus (PRP1 fixtures `222AAA`/`333BBB` copied into per-case dirs under `Deployments/Siara.Simulator/bulk_generated_documents_all_formats/`, gitignored) has inconsistent companions → fusion flags 1 conflict + ManualReviewRequired → export gate (`BlockOnUnresolvedConflicts`/`BlockOnFusionManualReviewRequired`, `ExportGatePolicy`) BLOCKS Stage 5 even at 90% classification. The original 2026-06-14 green used the *generated-consistent* `bulk_generated_documents_all_formats` corpus. Need a consistent case (reuse the fast-harness `AllRealWireThreeHostE2ETests` known-good case, or generate one).
- **Box setup done this session (reusable):** Playwright Chromium installed; `Microsoft.Playwright` 1.60 wants chromium-**1223** but ubuntu26.04 download is refused by PW's OS guard → symlinked `~/.cache/ms-playwright/chromium-1223 → chromium-1228` (works). Ubuntu-26.04 AppArmor blocks chromium's userns sandbox → owner set `sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0` (reversible; restore to 1 or reboot). SIARA sim runs via `dotnet Siara.Simulator.dll` from `Deployments/Siara.Simulator/app` with `Kestrel__Endpoints__Https__Url=http://localhost:5002` (forces http-only so the gate's plain HttpClient `IsSimulatorRunningAsync` doesn't choke on the self-signed https redirect) + `ASPNETCORE_ENVIRONMENT=Development`. Gate run cmd: `TESSDATA_PREFIX=/usr/share/tesseract-ocr/5/tessdata dotnet test <Tests.AllRealWireE2E.csproj> --filter-query "/*/*/MaxFidelityGateFullPipelineE2ETests/RealSiaraCase_FlowsAcrossAllThreeProcesses_WithRealPipeline_AndPersistsAudit"`.
- **NEXT:** owner-gated — resolve the native OCR-coexistence segfault (migration), then a consistent corpus case → the gate should go green (wiring, loader, tessdata all proven). Nothing else regressed.
**— PRIOR SESSION 2026-06-24b below. —**
**SESSION 2026-06-24b (BMAD-ORCHESTRATED THE TWO "NEXT (owner decision)" ITEMS — Branch `Liv`, all pushed, HEAD=d9d63523).** Owner picked: gate fork → **(b) real-TCP**, then refined to **rebuild off WAF**; audit bug → **widen to ~4k via disable-guard/alter/re-enable**. Four commits, each ground-truth-verified + adversarially reviewed:
- **`1a5dc11d` — audit `ErrorMessage` widen 1000→4000 (the incidental gate-log-line-293 `DbUpdateException`).** `AuditRecords` turned out to be an append-only **LEDGER** table guarded by the `TR_ProtectCriticalSchema` DDL trigger, so a naive `ALTER COLUMN` would crash prod at `MigrateAsync` boot. Migration `20260624120000_WidenAuditErrorMessageColumn` does **drop trigger → ALTER COLUMN → recreate byte-identical trigger** (all `suppressTransaction:true`). **Verified ledger-SAFE via MS Learn:** changing "the length of variable length columns" is a documented supported ALTER COLUMN on ledger tables — NO table rebuild, NO cryptographic-digest reset, data preserved. Config+snapshot+Designer at 4000 (ErrorMessage only). Infra.Database 0/0. EnsureCreated test path (gate/Testcontainers) gets nvarchar(4000) on a plain table → gate's over-long audit row now fits. Prod apply owner-gated.
- **`76b60566` — `Program.BuildApp(args, configureEarly?, configureServicesLate?)` seam on all 3 worker Programs (Orion/Athena/Reconciliator).** Pure extract-method + 2 optional no-op hooks; production behavior byte-identical. `configureEarly` runs right after CreateBuilder (inject config before the eager inline reads); `configureServicesLate` right before Build (remove/replace registrations). Verified: 3 workers 0/0; **Worker.Tests boot the real host via WAF<Program> → Orion 25/26 (the 1 fail = pre-existing env-gated `IngestionHubWireTests` SignalR-timing flake, NOT a regression), Athena 25/25, Reconciliator 8/8** — proves the refactor preserved wiring+entrypoint.
- **`d0f3dfdf` (SUPERSEDED) → `d9d63523` — gate rebuilt OFF WAF as single real-Kestrel hosts.** `d0f3dfdf` first tried a WAF **dual-host** (TestServer + Kestrel) — orchestrator caught in verification that it's **runtime-broken**: WAF `.Services` is the TestServer host but Athena connects to the *separate* Kestrel host, so the broadcast (published via `_orionApp.Services`) hits the wrong SignalR lifetime manager → event never delivered; + hosted services double-run (2× OCR). (Confirmed by `PrismaWebApplicationFactory`'s own docs: `.Services` = TestServer host ≠ the Kestrel host external clients reach.) **Fix = `d9d63523`:** `GateOrionHost`/`GateAthenaHost`/`GateReconciliatorHost` build the REAL worker wiring via `BuildApp` on a SINGLE Kestrel host each (UseUrls `http://127.0.0.1:0`), thread real `BaseAddress` downstream, drop all `HttpMessageHandlerFactory`/`Server.CreateHandler`; env-var conn-string hack removed (now via `configureEarly`). Publish + inbound connection now share ONE host → split-DI bug structurally gone, no doubling, real TCP keepalive + auto-reconnect. **`BuildThreeHostsWithDb`→async `BuildThreeHostsWithDbAsync`** (both test callers updated). Tests.AllRealWireE2E **0/0**; only the 4 gate files changed (fast `AllRealWireThreeHostE2ETests` + its WAF hosts untouched). **Adversarial review (plan-completion-reviewer): NO Blockers** — confirmed single-host delivery, no config-key drops, migration correct+reversible w/ verbatim trigger, async/dispose correct; 2 stale "in-memory transport" doc-comments it flagged are FIXED in `d9d63523`.
- **WHY THIS SHOULD FIX THE GATE:** the 2026-06-24 root cause was the in-memory SignalR seam dropping the event across a ~3-min idle download window under CPU contention (no keepalive). Real-TCP SignalR has keepalive + automatic reconnect, so the idle no longer drops the subscription. **CANNOT be proven on this box** (gate needs SQL + SIARA sim + Playwright + native Tesseract on an UNCONTENDED box; Docker still HARD-DOWN). On-box validation done = compile 0/0 + the BuildApp refactor's Worker.Tests + the fast harness still boots.
- **NEXT (owner): run the max-fidelity gate on a real/CI box** (`PRISMA_GATE_LOCAL_SQL` to local SQL2025, or Testcontainers) to confirm green over the new real-TCP transport. Tiny logged residuals (non-blocking): (i) gate DBs named `PrismaGate_*` aren't in the trigger's `PrismaTest_%` bypass — moot today (EnsureCreated installs no trigger); (ii) the audit widen migration is owner-gated for live SQL apply. The migration alone fixes the gate's audit overflow regardless.
**— PRIOR SESSION 2026-06-24 (ATHENA "OCR STALL" INVESTIGATED → REFRAMED) below; its "NEXT (owner decision)" items are the ones this session executed. —**

**SESSION 2026-06-24 (ATHENA "OCR STALL" INVESTIGATED → REFRAMED) — Branch `Liv`. Owner steer: "Investigate Athena OCR stall (hang vs slow)." Commit 3fd8a41c (serialization guards), pushed. Findings #1/#2 from 2026-06-23c also fixed (9cbeec30).**
- **THE GATE IS NOT BLOCKED BY ATHENA OCR OR CPU.** Definitive: the full gate3 console log (833 lines) has **ZERO extraction-stage markers** — no "Stage 1: Quality Analysis" / "Stage 2: OCR" / "Stage 3: Fusion" / "Error extracting" (all Info-level, would show). `ExtractionPipelineService.ProcessAsync` → `ExtractAsync` **NEVER RAN**. The earlier "Athena OCR env-ceiling / hang-vs-slow" framing (incl. prior memory) was WRONG.
- **ROOT CAUSE: the `DocumentDownloadedEvent` is never DELIVERED to Athena's ingestion handler.** Chain: Orion `SignalRIngestionBroadcaster.SendToAllAsync` → `Clients.All.SendAsync("ReceiveMessage", stamped)` (logs "Broadcast ... to all ingestion subscribers") → Athena `SiaraIngestionHubClient.On<DocumentDownloadedEvent>("ReceiveMessage")` → `IngestionEventForwarder.ForwardAsync` (logs "Forwarding..." Info or "rejected..." Warning) → local publish → `ProcessAsync`. In the gate, broadcast logged at 22:44:55 but **NO "Forwarding"/"rejected"/"clearance" log EVER** → `.On("ReceiveMessage")` handler never fired → event lost. The Athena ingestion client dropped 1s after the broadcast (22:44:56).
- **SERIALIZATION RULED OUT (committed guards 3fd8a41c):** a `DocumentDownloadedEvent` with 3 companion `CaseFiles` (SmartEnum `Format`) round-trips cleanly through (a) pure System.Text.Json Web-defaults + `EnumModelJsonConverterFactory` (`CaseFilesWireRoundTripTests` 2/2) and (b) the actual SignalR `JsonHubProtocol` envelope (`JsonHubProtocolCaseFilesTests` 1/1). Both server+client configure the converter (Orion Program.cs:195, SiaraIngestionHubClient:133). So it is NOT the populated-CaseFiles payload.
- **IT IS A DELIVERY failure on the IN-MEMORY SignalR test seam.** A live wire test (real Orion host + client, `server.CreateHandler()` transport) reproduced the drop **INTERMITTENTLY** (1 of 2 immediate-broadcast runs failed "not received in 10s"; a 40s-idle-then-broadcast run PASSED). The **gate's CONSISTENT** failure (3/3) most plausibly = the in-memory seam under the gate's specific conditions: **~3-min idle download window between connect and broadcast + heavy CPU contention (VMware workload) starving the transport's background pump**, neither of which an isolated unit test replicates. The **fast** harness (`AllRealWireThreeHostE2ETests` 29/29) passes because it broadcasts IMMEDIATELY (stubbed download → no idle).
- **IMPLICATION:** likely a **TEST-HARNESS limitation** (in-memory SignalR seam can't reliably span a multi-minute idle under contention), NOT necessarily a production defect (real-TCP SignalR has keepalive; OutboxRetryWorker/event-persistence add recovery). The gate as designed may be unable to PROVE the pipeline on this seam+box regardless of OCR. **Open product question:** does the real Orion→Athena edge guarantee delivery if the connection blips during a quiet period?
- **NEXT (owner decision):** (a) harden the gate — re-assert/refresh the Athena ingestion hub connection (or emit a keepalive) right before the broadcast so the long idle doesn't matter; (b) run the gate over a real-TCP transport (not the in-memory seam) so SignalR keepalive works; (c) run on a non-contended box; (d) confirm the production edge's delivery guarantee (reconnect + outbox replay). Also un-found: 1 incidental real bug — `AuditRecords.ErrorMessage` column truncates long messages → audit INSERT throws `DbUpdateException` (seen in gate log line 293; widen the column).
- **PRIOR 2026-06-23c (FINDINGS #1 & #2 FIXED) below.**

**SESSION 2026-06-23c (FINDINGS #1 & #2 FIXED) — Branch `Liv`. Owner steer: "Fix findings #1 & #2." Commit 9cbeec30, pushed.**
- **Finding #1 (per-file portal re-scrape) FIXED + Finding #2 (per-file browser leak) FIXED** in `SiaraDocumentDownloader.cs`. Both were real production issues, TDD'd (4 new tests), adversarially reviewed (plan-completion-reviewer → NO REAL ISSUES), committed.
  - #1: `PullDocumentAsync` now skips the full-portal `RetrieveDocumentsAsync` scrape when `documentId` is an absolute URL (the orchestrator always passes the case file URL from discovery). O(portal×files) → O(files). No ADR-010 weakening — `SelectDocument`'s absolute-URL fallback ALREADY downloaded such URLs without requiring presented-set membership (verified in prior code), navigate still establishes auth, DownloadedDocument still carries actor+session (P2 non-repudiation unchanged). Non-URL ids keep the scrape+gate.
  - #2: `DownloadAsync` wraps launch+acquire+pull in an OUTER finally that calls `_agent.CloseBrowserAsync` — balances each per-file launch with a close so Chromium instances don't accumulate (the adapter is `AddScoped`, reused across a case's files; `LaunchBrowserAsync` overwrote `_browser`/`_page` without disposing → leak → 3rd launch failed → dropped file → degraded `CaseFiles:2`). Close runs only AFTER the awaited download (downloads are sequential+awaited in `IngestCaseAsync`) so it can never tear down an in-flight download — **the owner's explicit concern**, addressed by choosing balanced-close-per-call over dispose-at-next-launch. Also closes a pre-existing acquire-failure leak.
  - Verified: Tests.Infrastructure.BrowserAutomation **218/218** (0 failed), prod build clean (warnings-as-errors).
- **GATE-GREEN STILL OPEN (separate from #1/#2):** the lean-sim run localized the remaining stall to the **Athena OCR/fusion stage** (Athena receives the event, silent ~12min, no `.fusion.json`). #1/#2 fix ingestion speed+robustness but do NOT touch Athena's OCR compute, so a re-run would very likely still hit that env-ceiling (CPU contention from the VMware workload) OR a possible true hang. Next: confirm hang-vs-slow (Debug extraction logs + bumped-timeout run) or run on a non-contended/CI box. The Docker-free local-SQL capability (66fd8e20) + lean-sim recipe make re-running easy.
- **PRIOR 2026-06-23b (DOCKER-FREE LOCAL-SQL RUN) below.**

**SESSION 2026-06-23b (§2 GATE — DOCKER-FREE LOCAL-SQL RUN) — Branch `Liv`. Owner steer: "Docker under repair, VMware not to be touched (important workload) — try to continue without Docker." Commit: 66fd8e20 (local-SQL gate path), pushed.**
- **Docker/WSL2 is HARD-DOWN** — `Wsl/Service/CreateInstance/CreateVm/0x800705b4` (ERROR_TIMEOUT); the WHOLE WSL2 VM layer fails (even plain Ubuntu cold-starts hang), NOT just the docker engine vhdx. Root cause this session = **VMware Workstation is running a live VM (`vmware-vmx`) contending for the host hypervisor** (the tracker's long-standing prime suspect, now confirmed). Tried + FAILED (all non-elevated): restart Docker Desktop, `wsl --terminate docker-desktop`, full `wsl --shutdown`, cold-start retry. `.wslconfig` is fine (the "unknown key" warnings are benign). Owner's `E:\Dynamic\Virtuals\WorkstationMaintenance\Repair-Docker-WSL.ps1` is GONE (path no longer exists). Real fix needs elevation or freeing the hypervisor (VMware) — owner-gated.
- **WORKED AROUND Docker via LOCAL SQL (committed 66fd8e20):** added an additive, env-gated path to `MaxFidelityGateE2EBase` — set `PRISMA_GATE_LOCAL_SQL` to a *master* conn string for a local instance (`DESKTOP-FB2ES22\SQL2025` 17.0.1115 + `\SQL2022` are both Running, reachable via Integrated Security) and the gate provisions/drops a GUID-unique isolated DB on it instead of Testcontainers. Default (CI/Docker) path unchanged. Build 0/0.
- **GATE RESULT (3 runs, all via local SQL, Docker-free): login + 3-host boot + ingestion + real-SQL audit ALL WORK — NO SQL collapse (prior Docker-era killer is GONE on local SQL).** But the gate still does NOT reach green — `ExportCompletedEvent(SiroXml)` never arrives within the 600s export window. Failure localized by 2 ground-truth methods (worker console logs captured via Tee, + watching the gate's shared-storage temp dir for stage artifacts pdf→fusion.json→siro.xml):
  - **Stall is the ATHENA OCR/FUSION stage.** Athena RECEIVES the `DocumentDownloadedEvent` (logs an audit row at receipt) then goes **silent for ~12 min** — no OCR/fusion/`ExtractionCompletedEvent`, no `.fusion.json` ever written → Reconciliator never gets a handoff → no export. EF command durations in the log are 100–1200ms (vs ~6ms idle baseline) → the box is **heavily CPU-contended** (the VMware workload). Most likely env-ceiling: native Tesseract+Emgu+PDFium can't finish 3-page OCR+fusion within 600s when starved 20×; a true hang isn't ruled out (would need Debug-level extraction logs + a long bumped-timeout run to distinguish). Corpus PDFs are 3-page ~380KB Skia/Chromium renders.
  - **REAL FINDING #1 (production, security-path, owner-gated — NOT changed):** `SiaraDocumentDownloader.PullDocumentAsync` re-scrapes the ENTIRE SIARA portal (`RetrieveDocumentsAsync`) on EVERY `DownloadAsync(documentId)` call, and the orchestrator calls it once per file → a 3-file case = 3 full-portal scrapes, each re-launching a browser. O(portal × files/case). Pathological when the served set is large (my over-warmed sim hit 100+ cases → discovery 353→377 files, ~2min/scrape, ingestion ate the whole budget). The scrape-then-select enforces "only download what SIARA presents to this session" (ADR-010) so it's NOT a pure optimization to remove — owner should decide. Mitigated for the gate by running the sim LEAN (reset cases.json + `SimulatorSettings__AverageArrivalsPerMinute=1.0` env → small, slow-growing set → fast scrapes; ingestion dropped from >6min to ~2min).
  - **REAL FINDING #2 (production robustness):** the per-file browser relaunch also threw `PlaywrightBrowserAutomationAdapter.LaunchBrowserAsync` on the 3rd file (XML) → case broadcast degraded `CaseFiles: 2` (best-effort ingestion still succeeded). Browsers aren't disposed between files within a case (class doc: "teardown left to DI scope") → resource pressure under load. Worth a per-file dispose.
- **SIM PRE-WARM RECIPE (works, Docker-independent):** reset `app/cases.json` to `[]`; start `Siara.Simulator.exe` from `app/` via `Start-Process -NoNewWindow` (survives across tool calls; `-WindowStyle Hidden` did NOT); auth via static-SSR form POST (GET `/login` → token; POST `_handler=loginForm`+`_model.Username=BANAMEX`+`_model.Password=password123`+`__RequestVerificationToken`); GET `/` fires `Dashboard.OnInitialized`→`CaseService.Start()` (prerender runs it; the singleton timer then serves regardless of circuit). Set arrival rate LOW so the served set stays small.
- **NEXT (owner decision):** (a) free CPU / run the gate on a non-contended or CI box to get true green; (b) confirm hang-vs-slow with Debug extraction logs + a 30–40min bumped-timeout run; (c) fix findings #1/#2 (real production issues, independent of the box); (d) accept the env-ceiling — now sharply characterized (SQL ruled out; localized to Athena OCR/fusion under VMware CPU contention). The local-SQL gate capability (66fd8e20) is reusable for any future Docker-down run.
- **PRIOR SESSION 2026-06-22/23 (§2 RE-RUN, Docker) below.**

**SESSION 2026-06-22/23 (§2 MAX-FIDELITY GATE RE-RUN) — Branch `Liv`. Owner steer: restore corpus + run §2 gate. Commits: 5d76faea (calib-report CL-48), e31a232a (Serilog DOA fix) — both pushed.**
- **Corpus:** `bulk_generated_documents_all_formats` is PRESENT (500 cases, full PDF+DOCX+XML companions) — owner had already copied it back. `app/cases.json` is the sim's *served-set* persistence (git-tracked runtime artifact — hygiene smell, left as-is; 500 avail >> served so non-blocking). The 06-20c "corpus MISSING" blocker is RESOLVED.
- **Docker:** was BROKEN AGAIN (`CreateInstance/CreateVm/0x800705b4`, engine distro boots-then-hangs) **even after a clean reboot** — so NOT the Modern-Standby theory. Owner repaired via `E:\Dynamic\Virtuals\WorkstationMaintenance\Repair-Docker-WSL.ps1` (rebuilds the engine distro; data safe in separate docker_data.vhdx). **OPEN: recurring engine-vhdx corruption root cause** — prime suspects Defender real-time scanning the WSL vhdx and/or VMware host-mode contention; an elevated read-only agent was briefed to check `hypervisorlaunchtype`/Defender exclusions/auto-update. DockerAutoHeal task IS registered. Durable fix likely a `Set-DockerDefenderExclusion.ps1` trio.
- **REAL BUG FIXED (commit e31a232a) — G-S2 Serilog MSSqlServer audit-log sink was DEAD-ON-ARRIVAL in all 4 services (Orion/Athena/Reconciliator workers + Web.UI), crashing host boot.** Two stacked defects, both surfaced ONLY by the gate booting real hosts (green unit tests ≠ wired system): (1) `sinkOptionsSection` was a STRING path not an inline object → Serilog binder threw "Type ... was not found"; (2) underneath, Serilog.Sinks.MSSqlServer v8 constructs EAGERLY and throws ArgumentNullException on null/empty connectionString — it does NOT "skip when blank" as the appsettings comment claimed; `%SERILOG_SQL_CONNECTION%` was empty in dev/test/gate. **Fix:** moved the SQL sink out of always-on appsettings into CODE, conditional on a real DefaultConnection (activates in prod/gate w/ real SQL, cleanly skipped in dev). Verified: Web.UI 0/0; gate now boots all 3 hosts + runs pipeline end-to-end (download→quality→OCR).
- **§2 GATE STATUS: ALL CODE ISSUES FIXED; full-green is ENV-CEILINGED on this box (6 runs this session, do NOT keep retrying as-is).** Progression: run 3 → Serilog boot crash (FIXED, e31a232a); run 4 → SQL collapse on cold Docker; run 5 → SQL baseline FIXED by owner's Docker/WSL patches (0 SQL timeouts) but COLD sim served 0 cases → login "no PDF link in 90s"; run 6 → pre-warmed sim, **login PASSED + pipeline discovered+downloaded a case**, but SQL **collapsed AGAIN under the gate's concurrent load (84 SQL timeouts, "connection is closed")** → 25-min timeout.
  - **SQL is fast IDLE (~6ms/insert, proven by standalone smoke test) but degrades under the full-gate concurrent load** (3 worker hosts + Playwright + native Tesseract OCR + Testcontainers SQL on a CPU-throttled box). NOT a code bug — resilience layer (OutboxRetryWorker/ResilientSLAEnforcer) handles it correctly.
  - **Sim cold-start root-caused:** `CaseService.Start()` is deferred to `Dashboard.OnInitialized` (`Components/Pages/Dashboard.razor:242`); document links render from `_cases` populated on the INTERACTIVE circuit, so a COLD sim prerenders 0 links (headless circuit too flaky in 90s) while a WARM sim (active cases) prerenders links directly. Runs 3/4 passed login only because they reused a warm sim. **Pre-warm recipe (proven this session):** start sim from `app/`, authenticate (static-SSR form POST: GET `/login` for antiforgery token → POST `Model.Username=BANAMEX`/`Model.Password=password123`/`_handler=loginForm`/`__RequestVerificationToken` → cookie), GET `/` (root = dashboard) to fire `OnInitialized`→`Start()`→Discover 500→serve; wait ~75s for ~13 active cases; then run the gate (it reuses the running sim on :5001). Reset `app/cases.json` to `[]` first (git-tracked served-set; accumulates → 1420-file discovery bloat).
  - **To get §2 green (all ENV/owner, no code):** run WorkstationMaintenance `Set-CpuTurboPerformance.ps1` (box turbo pinned at base clock = the root starvation), raise WSL2 CPU/mem in `.wslconfig`, optionally bump EF command timeout + the SLAEnforcer Polly 5s timeout so ops tolerate slow-SQL-under-load instead of failing. Or run the gate on a beefier/CI box. Timeout cap already 15→25 min (9ad386e8).
  - **PLANNED (not done) — simulator roadmap `docs/planning-artifacts/siara-simulator-load-roadmap.md` (commit 28f24f0c):** idea 1 = auto-start CaseService at boot (removes the dashboard/circuit dependency → no manual pre-warm) + auto-replenish when consumed + cases.json hygiene; idea 2 = scale corpus to ~10k via `Prisma/PRP/PRP1/research/generators/AAAV2_refactored/` (`batch_generate.py`, ~11.5GB, ~5-7h) + burst arrival model (~2,500/day in bursts).
  - **Net session win: the committed Serilog boot-crash fix (real latent production bug in 4 services). §2 itself stays owner-deferred — env-ceilinged, not code-blocked.**
- **PRIOR (2026-06-20c POST-DOCKER-REBOOT) below — runbook drained.**

**WAVE-1 SESSION 2026-06-20c (POST-DOCKER-REBOOT) — Docker LIVE (29.5.3). Ran the post-reboot runbook (HANDOFF-2026-06-20b-POST-DOCKER-REBOOT.md). Branch `Liv`, HEAD=da375c2e. 3 real findings fixed/surfaced.**
- **§1 Testcontainers SQL smoke — DONE (Docker proven):** Tests.System.Storage **48/48** (3 first-run fails were cold-Ollama LLM-inference warmup flakes; warm re-run all green — env, not regression). Tests.Infrastructure.Database **169/169**. Veriqan persistence IT **7/7** after fixing **1 REAL FINDING (commit e8a08ccf):** `VerdictPersistenceIntegrationTests.PersistAsync_HappyPath` inserted a JobVerdict+Findings for a random jobId with no parent VerificationJob → real SQL rejected it on FK_JobVerdicts/Findings_VerificationJobs (cascade FK from the VerificationJob aggregate; InMemory doesn't enforce FKs). Production correct; test arrange seeded the parent. LESSON: InMemory-green ≠ real-SQL-green for any FK/constraint.
- **§2 max-fidelity E2E gates — BLOCKED, owner-deferred:** both MaxFidelityGate scenarios fail at the 90s login PDF-link wait (reproduced solo on idle box → NOT load). Root cause = the SIARA sim's curated corpus `Prisma/Deployments/Siara.Simulator/bulk_generated_documents_all_formats/` is **MISSING** (gitignored generated data, present when gate last green 2026-06-14; gone post-reboot). Sim discovers 0 cases → "Simulation finished" → no PDF link. cases.json stale (48 served). Pipeline+sim code FINE; pure data gap. A repo doc warns vs blind regen. **Owner ruled: defer §2, restore corpus later then re-run gate.** ProcessId-audit AC + OCR-deadlock-regression scenario deferred with it.
- **§3 FU1 broadcaster→DOM bridge — DONE + PROVEN LIVE (commit da375c2e). Owner chose option (a).** Implementing+proving it surfaced that the live-update path was **fully non-functional (real bug), not just missing a listener:** `IExxerHub<DomainEvent>` was registered as the ProcessingHub CLASS itself; Ember's `ExxerHub.SendToAllAsync` uses the Hub instance `Clients`, which is NULL on a DI-resolved hub → broadcaster's SendToAllAsync silently no-op'd, nothing reached the browser. Fix = new `HubContextDomainEventBroadcaster` (IHubContext-backed IExxerHub adapter, mirrors Orion's SignalRIngestionBroadcaster, emits "ReceiveMessage"); broadcaster unchanged (still transport-agnostic). Razor: `On<object>("ReceiveMessage")` → reload + **StateHasChanged** (reloads only set fields; hub callbacks don't auto-render). Also fixed pre-existing broken test infra: `PrismaWebApplicationFactory.EnsureStarted/CreateHost` NRE'd for this minimal-hosting app (returns Kestrel host where WAF wants TestServer); rewrote to the Andrew-Lock dual-host pattern + `HostedServices` accessor (publish on the Kestrel host the browser connects to, NOT factory.Services=TestServer). **PROVEN:** un-skipped `Dashboard_ReceivesLiveMetricsUpdate_…` Playwright test — DOM goes 5→6 on a published DomainEvent with no reload; + new server-only `Broadcaster_DeliversReceiveMessage_ToConnectedSignalRClient` diagnostic. Tests.UI 25/27 (2 fails = PRE-EXISTING env-gated NavigationSmoke, standalone-UI-process /health timeout w/o SQL — NOT regression). LESSON: a Blazor live-update needs ALL of {IHubContext broadcast, matching wire method, StateHasChanged, interactive rendermode}; verify each leg (server-only diagnostic isolates transport from circuit/DOM).
- **§4 docker build — DONE (commit d33adced):** all **4 production images build locally** (prisma-orion/athena/reconciliator/webui). Found+fixed **1 REAL deployability bug:** the Web.UI image build failed MSB3030 — the csproj copies a golden-test-demo fixture PNG from `Prisma/Fixtures/PRP1/` via `..\..` Content link, but the Dockerfile context is only `Prisma/Code/Src/CSharp/` → file absent → publish failed. Fixed with `Condition="Exists(...)"` on the ItemGroup (local demo unaffected; Docker/clean-checkout skips it). Full `docker compose up` deferred (heavy + sim service is corpus-gated like §2). LOGGED minor product quirk (out of scope, not fixed): Dashboard.UpdateChartDataWithRealTrends zeros ALL metric cards when recentEvents is empty (masks TotalDocumentsProcessed>0) — revisit if a fresh dashboard shows 0s despite processed docs.
- **§5 PRISMA-E2-S8 CI publish — DONE (commit d33adced):** `publish` job added to quality-gates.yml — matrix over 4 hosts; resolve registry (REGISTRY_URL secret → ghcr.io default), login (explicit creds or GITHUB_TOKEN), build+tag SHA+latest, **trivy CRITICAL scan**, push; **main-only**, permissions packages:write. YAML validated; build leg proven by the §4 local smoke builds. Registry push stays CI-credential-gated. Closes D24.
- **ADVERSARIAL REVIEW (plan-completion-reviewer, this session) on the FU1 + Veriqan commits → 1 Major + 2 Minor + 1 Nit, ALL FIXED (commit 599a3eea); product code confirmed correct:** (Major) FU1 Playwright test only asserted the DOM eventually showed "6" — could pass via a non-SignalR re-render; added a **negative control** (after flipping the fake but before publishing, assert DOM REMAINS "5" — Dashboard has no poll timer → the flip to "6" is provably caused by the event). (Minor) factory leaked the TestServer host if Kestrel start threw → try/catch dispose. (Nit) Veriqan seed status Completed→Pending (matches real ingestion precondition). Reviewer confirmed: no other consumer depends on IExxerHub<DomainEvent>=concrete ProcessingHub; adapter correct vs Orion template; Veriqan fix is the right repair (production always persists the job before the verdict).
- **§6 full Playwright numbers — DONE, NO REGRESSIONS from this session's changes:** Tests.UI **25/27** (2 fails = PRE-EXISTING env-gated NavigationSmoke standalone-process /health timeout w/o SQL). Tests.EndToEnd **27/27** ✅ (confirms the Program.cs IExxerHub-registration + csproj changes didn't break DI/wiring). BrowserAutomation.E2E **31/33** — the 2 fails (`DownloadAsync_…PullsRealDocumentFromSimulator`, `DiscoverCasesAsync_…ListsRealCasesFromSimulator`) are the SAME missing-SIARA-corpus gap as §2 (identical "no PDF link within 90s" message), NOT a regression.
- **POST-REBOOT RUNBOOK ESSENTIALLY DRAINED.** §1 ✅+1fix(Veriqan FK), §2 owner-deferred(corpus), §3 ✅ FU1 implemented+PROVEN+real-bug-fixed, §4 ✅+1fix(Web.UI MSB3030), §5 ✅(CI publish), §6 ✅(no regressions). Commits this session: e8a08ccf (Veriqan FK test), da375c2e (FU1 bridge), 599a3eea (review fixes), d33adced (Web.UI Docker + CI publish). All pushed (push tracker+memory next). 3 real findings fixed (Veriqan FK / FU1 broken broadcast / Web.UI Docker), 1 owner-deferred (§2 corpus), 1 logged minor (empty-recentEvents zeros dashboard cards).
- **NEXT SESSION:** §2 gate re-run once owner restores `bulk_generated_documents_all_formats` corpus (+delete stale app/cases.json). Full `docker compose up` (heavy; sim service corpus-gated). Still GATED (unchanged): PRISMA-E3 security (biz/ops), PRISMA-GATED ingestion (legal/corpus), PRISMA-E4 PersonIdentityResolver (buildable-now, future), Veriqan-E3+ (issue #17). HEAD after wrap-up commits TBD (was d33adced).
---
**WAVE-1 SESSION 2026-06-20b DONE — PRISMA-E5 CLEANUP EPIC (S1–S5) landed + adversarially reviewed (2 skeptics) + 4 review-fixes (2 Majors) + pushed. Branch `Liv`, HEAD=e1a6658f. Owner steer = "Drive E5-S1..S5 now" + "S2 SignalR re-enable IN-SCOPE".**
- **5 stories (S3 doc + S1/S5 disjoint parallel, then S2):**
  - **S1 (e0883ea8):** GROUND-TRUTH REFUTED the brief's "legacy dead ROP path" premise — `ProcessingOrchestrator` is LIVE (AddSingleton `Athena.Worker/Program.cs:115`, full pipeline). So documented (KNOWN LIMITATION comment on the `FuseAsync(null,pdf,null)` single-source-fusion call) instead of `[Obsolete]`. EfCoreIdentityAdapter: added ADR-012 §Context/§Decision-2 comment (intentionally unregistered; confirmed zero DI registrations).
  - **S3 (bba34ebf):** CLAUDE.md de-staled — project count 200+→~70 (33+37); removed Counter/Weather caveat (absent); rewrote Orion ingestion bullet (StubDocumentDownloader/StubExxerHub dead, SiaraDocumentDownloader@Program.cs:80 + SiaraWatchLoop@:218 real, StartAsync placeholder gone) — EVERY claim cross-checked vs Orion.Worker/Program.cs; RC5 pointer added.
  - **S4 = ALREADY DONE (verify-only):** holiday calendar was implemented as Item-G #13 — IBusinessDayCalculator + MexicoBusinessDayCalculator (PublicHoliday pkg) wired into SLAEnforcerService + FusionExpedienteService, DI-registered in 3 roots, ACs covered. Tests.Infrastructure.Calendar green. No code.
  - **S5 (041e2d95):** DocxFieldExtractor SAT regex hardening (RFC/folio/fecha/autoridad, named groups, CultureInvariant) + 17 tests. Closes D2/issue#2.
  - **S2 (1b34de2b):** owner-ruled IN-SCOPE → re-enabled `AddHostedService<SignalREventBroadcaster>` (Web.UI Program.cs; IExxerHub<DomainEvent>→ProcessingHub IS registered@:188 so no boot/runtime crash; 2025-12-08 disable reason stale). Hardened GetRequiredService→GetService. Honest Playwright test: negotiate probe + **deliberately-skipped** live-update test.
- **ADVERSARIAL REVIEW (plan-completion-reviewer + qa skeptic) → 2 Majors + minors FIXED (2d1068fc S5, e1a6658f S2):**
  - **S5 Major (RFC first-match bias):** optional "del contribuyente" qualifier → flat-joined doc with "RFC del representante legal:" before "RFC del contribuyente:" returned the REPRESENTATIVE's RFC. Fixed two-pass (exact contribuyente → bare `RFC(?!\s+del\s)`). +multi-RFC test.
  - **S5 Major (Fecha flat-join contamination):** `ExtractTextFromDocx` space-joins runs → no newlines → `[^\n\r,;]{5,40}` grabbed into next field ("09 de Abril de 2025 RFC del"). Fixed to date-shaped token (Spanish/ISO/numeric), label-anchored. +contamination + true-negative tests.
  - **S2 Major (async-void Rx onNext):** a SYNC throw before first await → Rx onError → Subject dies → ALL later events silently dropped. Fixed: sync onNext fires `SafeBroadcastAsync` (try/catch contains everything). +null-guard moved to top.
  - **S2 Minor (Tests.UI factory):** PrismaWebApplicationFactory now strips the broadcaster (mirrors E2E TestWebApplicationFactory) so the shared Tests.UI host has no live Rx subscription; negotiate test re-scoped (option b) to assert MapHub endpoint + split out of Playwright class.
- **CONFIRMED GAP — FU1 (BOTH reviewers, owner-gated, NOT fixed):** broadcaster emits via Ember's generic `SendToAllAsync` wire method name; Dashboard.razor/SlaDashboard.razor listen for TYPED names "MetricsUpdated"/"SLAStatusUpdated"/"SLAEscalated" (only ProcessingHub's typed methods emit those). So server push + client hub connections both work but NO DOM updates from a DomainEvent. Decision pending owner: (a) add Razor `On(<ember-name>)` handler, or (b) broadcaster casts to ProcessingHub + calls typed methods by event type. Tracked as task #8.
- **Test gates (re-verified independently from ground truth):** Tests.Infrastructure.Extraction **478/478** (469+9), Tests.Infrastructure.Calendar green, Tests.UI build 0/0 + 23 passed/1 skipped(FU1) [2 NavigationSmoke fails = PRE-EXISTING env-gate: no SQL→/health 503, per tracker line 98, NOT a regression], Web.UI build 0/0, Auth.Infrastructure/Athena.Processing/Athena.Worker builds 0/0, Tests.EndToEnd DependencyValidationTests 27/27 (subagent, SQL-box).
- **DEFERRED (env-gated, code-complete):** negotiate-endpoint live assertion (SQL+Playwright), FU1 live-update Playwright test (skipped, needs the bridge + browsers). Pre-existing: 2 NavigationSmoke env-gate.
- **NEXT:** PRISMA-E5 non-gated DONE. Remaining Prisma Wave-1 = E2-S8 (CI docker publish — Docker/CI-gated) only; rest is E3 security (business/ops-gated), E4 PersonIdentityResolver, PRISMA-GATED ingestion (legal/corpus). **FU1 awaits owner decision.** Veriqan-E3+ ROADMAP-gated on issue #17.
- **OWNER DECISION 2026-06-20b: PAUSE + HAND OFF. Docker was REPAIRED on the box but needs a REBOOT to activate.** Owner deferred FU1 to a Docker/Playwright-verifiable session (post-reboot). **→ Post-reboot runbook written: `docs/planning-artifacts/remediation/HANDOFF-2026-06-20b-POST-DOCKER-REBOOT.md`** — prioritized list of every Docker/SQL/Playwright-gated deferral now unblockable (Testcontainers SQL suites, max-fidelity E2E gates incl. the re-enabled OCR scenario + ProcessId audit AC, FU1 implement+verify, docker build/compose, PRISMA-E2-S8 CI publish, full Playwright numbers). START THERE next session after `docker info` confirms the daemon is live.
---
**WAVE-1 SESSION 2026-06-20 DONE — PRISMA-E2 NON-GATED TRIO (S3, S5, S4) landed + adversarially reviewed + 1 review-fix + pushed. Branch `Liv`, HEAD=1b11a1ee. Owner steer = "Drive Prisma-E2 (non-gated)".**
- **3 stories (S3∥S5 parallel disjoint trees, then S4):** S3 (62021cbb Sentinel traced→classified **(b) partial scaffold**: real logic + 16 tests BUT no Program.cs/host, not DI-registered anywhere, IProcessRestarter has no impl, no worker emits heartbeats → OUT-OF-MVP; doc `docs/operations/SENTINEL-STATUS-2026-06.md` + CLAUDE.md services-table note). S5 (f4625b26 D22 ProcessId): **ground-truth finding = already done** — all 3 prod LogAuditAsync call sites (IngestionOrchestrator:610, ExtractionPipelineService:326, ReconciliationPipelineService:248) ALREADY pass `processId: actorId` from ISiaraActorIdentityProvider (ProcessingOrchestrator has none — delegates); story reduced to adding the regression assertion (every audit row ProcessId!=null) to MaxFidelityGateFullPipelineE2ETests. S4 (b9ecd85a D21 Tesseract deadlock): root cause = `new TesseractEngine` per OCR call → native second-init deadlock; fix = lazy singleton engine + SemaphoreSlim(1,1) + IDisposable in TesseractOcrExecutor, DI Scoped→AddSingleton, re-enabled the disabled MaxFidelityGatePartialCaseE2ETests scenario (runAthenaPipeline:true).
- **ORCHESTRATOR CAUGHT 1 Major DURING S4 verify (before review):** the subagent left the keyed("Tesseract") + unkeyed IOcrExecutor as TWO separate concrete singletons → two engines if both resolved in one process = the exact deadlock reborn. Forwarded both to ONE concrete `AddSingleton<TesseractOcrExecutor>()` + added a DI same-instance regression test. (Note: Athena.Worker/Program.cs:94 registers the executor directly, not via AddExtractionServices — no overlap, but the extension is the canonical path and had to be correct.)
- **ADVERSARIAL REVIEW (plan-completion-reviewer vs epic briefs) → ZERO Majors; 2 Minors + 1 Nit, all S4, all FIXED (1b11a1ee):** (Minor, correctness) Dispose() race — caller past entry _disposed guard could grab the lock after Release() but before _engineLock.Dispose() and lazy-create an engine on a shutting-down host; added a _disposed re-check INSIDE the locked region + made _disposed volatile. (Minor) stale doc comment in MaxFidelityGateFullPipelineE2ETests.cs:51-53. (Nit) stale rationale in MaxFidelityGateHosts.cs runExtractionPipeline guard. Review CONFIRMED: no other `new TesseractEngine`, no other double-registration, S5 call sites correct, S3 (b) classification sound.
- **Test gates (re-verified independently from ground truth):** Tests.Infrastructure.Extraction.Teseract **159/159** (154 existing OCR + 4 deadlock-regression + 1 DI same-instance), run TWICE (after S4, after review-fix); Tests.AllRealWireE2E build **0/0**; Orion.Ingestion build 0/0, Athena.Processing build 0/0; Sentinel.Monitor.Tests 16/16. (S5/S4 subagent-reported: Orion.Ingestion.Tests 55/55, Athena.Processing.Tests 105/105.)
- **DEFERRED (Docker/Playwright-gated, code-complete):** the re-enabled MaxFidelityGatePartialCaseE2ETests OCR scenario + the S5 ProcessId E2E assertion both need Testcontainers SQL + Playwright + native Tesseract → run on a CI box. **Deferred MINOR (logged):** TesseractEngine binds to the FIRST call's language/OEM/whitelist — fine for this Spanish-only pipeline (documented in the class XML doc); revisit only if multi-language OCR in one process is ever needed.
- **NEXT:** PRISMA-E2 non-gated trio DONE. Remaining Prisma Wave-1 = E2 **S8** (CI docker publish — Docker/CI-gated, NOT startable here) + **E5 cleanup S1–S5** (several Docker-gated; some buildable-now). Veriqan-E3+ still ROADMAP-gated on issue #17. Owner steer needed: tackle Prisma-E5 cleanup (non-gated bits) or pause.
---
**WAVE-1 SESSION 2026-06-19c DONE — VERIQAN-E2 PdfPig serial tail FULLY DRAINED (S5,S6,S9,S10,S11) + E2-boundary adversarial review (2 skeptics) + 3 Majors remediated + pushed. Branch `Liv`, HEAD=d70e4571. VERIQAN-E2 is now 16/16 + both cosmetics — EPIC COMPLETE.**
- **5 stories landed (one-per-round PdfPig serial chain):** S5 (8ba45202 CL-35 Aptos StartsWith + FontUsage.IsEmbedded), S6 (391e7426 CL-34 image-only-gated page-1 propagation + masked last-4), S9 (923c8b91 America/Mexico_City year-repair + VeriqanConstants), S10 (7341b5f0 es-MX number-format AmountNumberFormatSession), S11 (7e771d2c CL-48 whitespace-glyph filter + 2cm intra-page gap + PageInspectionFacts.MaxVerticalGapPoints). Plus 9ce99402 (repo-hygiene: dropped an accidentally-committed agent-worktree gitlink, ignored .claude/worktrees/).
- **ORCHESTRATOR CAUGHT 2 green-tests-missed Majors DURING the drain (before review):** (1) S10 first cut abstained on ANY lone single-separator amount → a small-balance statement (<$1,000, no thousands comma to seed detection) failed to extract EVERY amount; fixed with two entry points (TryParseUsMx for the ~6 regex-gated US/MX-shaped sites, never abstains; detecting TryParse only at the unfiltered §20 path). (2) S11 2cm-gap check newly false-failed all 3 KnownSynthetic specimens — investigated: TRUE positive (they're missing mandatory sections per LAW-SEC-PRESENCE + non-standard spacing per LAW-SEC-ORDER-GAP → genuine >2cm blank bands); registered CL-48 as a knownFixtureDefect in corpus-manifest.json with reason (the harness's prescribed resolution), not a rule fix. The Orchestration CalibrationDriverTests harness is the real false-fail gate — RUN IT after any rule/extractor change.
- **E2-BOUNDARY ADVERSARIAL REVIEW (plan-completion-reviewer + qa skeptic) → 3 Majors FIXED:** (1) S5 (99382e4a) — empty-family Type0 run did `return InsufficientData` INSIDE the loop, discarding an already-found offender → false-PASS ([Arial,Type0]→abstain); now `continue` + abstain only if evaluableRuns==0; also boundary-guard on StartsWith (AptosCustom no longer false-PASSes). (2)+(3) S6 (d70e4571) — masked CardGroupPattern `[\dX]{4}` let an all-digit transaction ref ending in the card last-4 false-PASS CL-34 (tightened to mask glyphs `[Xx*•·●]`, extracted testable TryMatchMaskedCard helper); page-1 propagation hardcoded `PageNumber==1` → false-RED on sliced statements (now MinBy(PageNumber)).
- **Test gates (all re-verified independently from ground truth):** Visual.Tests 164/164, Extraction.Tests 166/166, Orchestration.Tests 71/71 (calibration green), Domain/Visual/Extraction builds 0/0.
- **DEFERRED MINORS (logged, non-blocking):** (a) S5 IsEmbedded extractor wiring (field present, defaulted false — PdfPig Letter doesn't expose per-letter embed status). (b) S9 Windows uses US-Central "Central Standard Time" not "Central Standard Time (Mexico)" — post-2022 MX has no DST; does NOT affect year-repair (year flips only at Jan-1 where both are UTC-6); tighten the Win ID as cheap hardening if revisited. (c) S10 §20 amount format unseeded if ALL period-summary+movement amounts are Missing (inconsistent §20 scale; §20 not yet rule-gated → no current wrong verdict). (d) S10 no end-to-end synthetic-PDF test with all sub-$1000 amounts (covered at session-unit level). (e) S11 2cm-gap threshold UNCALIBRATED — no KnownGood corpus to prove no over-fire on a compliant statement with legit section whitespace; safe direction (over-flag → human review, never false-PASS); calibrate in E5. (f) S11 band-merge within 5pt tolerance could slightly shrink a measured gap (potential false-PASS on a borderline ~57pt band).
- **NEXT:** VERIQAN-E2 epic is DONE. Remaining Wave-1 = **Prisma-track** E2 S3(Sentinel trace)/S5(ProcessId audit)/S8(CI docker)/S4(Tesseract deadlock — OCR infra) + Prisma-E5 cleanup S1-S5 (several Docker-gated); and Veriqan-E3+ (productization, ROADMAP-GATED on issue #17). Owner steer needed: continue into the Prisma track (non-gated bits) or pause. Prisma ⟂ Veriqan so a Prisma story CAN parallel a Veriqan one.
---
**WAVE-1 SESSION 2026-06-19b DONE + ADVERSARIALLY REVIEWED + 3 MAJORS REMEDIATED + pushed. Branch `Liv`, HEAD=6e429a05. Owner steer = "resume Wave-1 draining".**
**6 stories landed this session:** S7 (f15d4f10 footer-band pagination), S13+CosmeticS2 (c9640ef9 IVA→ILegalToleranceProvider.IvaRate), S16 (e9fc1287 GetOrAdd+retry dedup), CosmeticS1 (422155cd VerdictSignal doc), S4 (dd9a3b7a FR-12 pHash CatalogImagePresenceRule + gated extraction-stage render→hash).
- **S4 nuance:** first pass delivered the rule but NOTHING populated StatementModel.PagePerceptualHashes (rule永abstained — non-functional). Orchestrator caught it from ground truth (grep: only written in a test) and re-delegated the render-population. Now wired + gated behind ctor flag `enableCatalogImageHashing` (default OFF — all-page PDFium render is costly; enable once a real catalog-image bundle + corpus-calibrated Hamming threshold land, VERIQAN-E5/CPA-2). PDFium verified rendering on-box (3/3 non-zero hashes).
- **ADVERSARIAL REVIEW DONE (2 skeptics: plan-completion-reviewer + qa-correctness) → 3 Majors FIXED (81c9e328 extractor, 6e429a05 persistence):**
  (1) **S7** footer band was a HARD filter → a 'N de M' label just above the 10% band was silently lost → CL-31 abstain → false-PASS. Now footer is a PREFERENCE with page-wide last-match FALLBACK (recall never worse than pre-S7). +boundary test (Y~12%).
  (2) **S4** SKBitmap bytes reinterpreted as Bgra32 assuming PDFium=Bgra8888; RGBA platforms → channel swap → false-RED on every image once live. Now normalized to Bgra8888 (zero-copy on Windows) before LoadPixelData.
  (3) **S16** EF dup-retry re-queried the SAME poisoned context → identity map could return the uncommitted NEW entity (wrong GUID) → idempotency broken on real SQL (InMemory test blind to it). Now ChangeTracker.Clear()+AsNoTracking() re-query; honest WithFailure on non-constraint DbUpdateException. +non-constraint test.
- **Verified integrated by orchestrator (re-run, ground truth):** Validation.Tests 479/479, Extraction.Tests 145/145, Orchestration.Tests 71/71, Application.Tests 119/119. (warnings-as-errors ⇒ green = production 0/0.)
- **Concurrent-edit collision (S13+S16 both edited Section6PaymentSimulationRule.cs) merged coherently — VERIFIED by integrated build+test.** S16 also fixed a CS1574 S13's new doc introduced.
- **Calibration-report.md regenerates a fresh TIMESTAMP on every Orchestration.Tests run — pure churn; reverted, do NOT re-commit. (repo-hygiene smell: tracked generated artifact.)**
- **DEFERRED MINORS (logged, not fixed — none are false-verdict risks now):** (a) S7 10% threshold still wants real-corpus calibration → E5 (mitigated by fallback). (b) S13 `IvaRate=0` from a MISCONFIGURED provider → Section16 false-RED; both shipping providers return 0.16 — add a `>0` guard as cheap hardening if revisited. (c) S4 partial page-hash list (a matching page that fails to render → false-absent) → E5; gated OFF. (d) S4 true E2E round-trip test (rendered hash → rule PASS) → E5/corpus.
- **VERIQAN-E2 STATUS: 11/16 + both cosmetics DONE.** Done: S1,S2,S3,S4,S7,S8,S12,S13,S14,S15,S16. **Remaining E2 = 5, ALL PdfPig serial-chain (one per round): S5 (CL-35 Aptos font + FontUsage.IsEmbedded), S6 (CL-34 card image-fallback/page-1 propagation), S9 (America/Mexico_City timezone), S10 (es-MX number format), S11 (CL-48 blank-page glyph filter + 2cm gap).**
- **NEXT:** drain E2 PdfPig tail S5→S6→S9→S10→S11 one-per-round (all touch PdfPigStatementFieldExtractor.cs — never parallel with each other; CAN parallel with a Prisma-tree story, Prisma ⟂ Veriqan). Then Prisma: E2 S3/S5/S8/S4 + E5 S1-S5. Run adversarial review at the E2-epic-completion boundary.
**WAVE-1 (prior) — owner steer 2026-06-19 = "both tracks in parallel". 9 stories DONE + 2 Major reworks, all ground-truth-verified + adversarially reviewed + remediated. Was HEAD=323ff4a5.**
- **DONE (Veriqan-E2 cardinal/correctness, 7):** S1 text-density abstain guard (60fe635d), S2 §20 saldo-a-favor sign guard (d0ab2485; commit subject mislabels it "S1" — it IS S2/#2), S3 §16 column-count guard (8df83b01), S8 TimeProvider→**reworked** to anchor on PeriodCutDate (aa998bdd + fix), S12 VerdictAggregator unknown→abstain (dcc3ec73), S14 marked-PDF rotation→**reworked** swapped-90/270 fix (1adfcbc9 + fix), S15 dup-alert AlertSentAt+concurrency-token (5e1eea27).
- **DONE (Prisma-E2 buildable-now, 2):** S2 production runbook (62ba0c2a; corrected migration-ownership: --migrate-only does ONLY PrismaDbContext per worker), S9 ADR-013 accept in-memory metrics for MVP (95161817).
- **ADVERSARIAL REVIEW (2 skeptics vs epics briefs) → 2 confirmed Majors REWORKED + pushed (323ff4a5):** (1) S14 — the 90° and 270° coordinate transforms were SWAPPED, placing highlights outside the MediaBox on A4; old 270° test bounds-check (<=842) masked it. Re-derived all 4 + added 36-case bounds-invariant property test (Reporting.Tests 67/67). (2) S8 — TimeProvider.System default still drifted across year boundary on reprocess (#36 NOT closed); now anchors year-repair to statement PeriodCutDate year, TimeProvider fallback; cross-clock determinism test (Extraction.Tests 140/140). Minors documented not reworked: dup-alert is DELIBERATE at-least-once (duplicate RED email benign; never drop a RED) — comment added; VerdictAggregator GREEN-with-InsufficientData is by-design (test asserts PassCount=0).
- **Wave-1 test gates (all re-verified independently from ground truth):** Validation.Tests 473/473, Application.Tests 117/117, Reporting.Tests 67/67, Orchestration.Tests 69/69, Extraction.Tests 140/140, Persistence builds 0/0.
- **PdfPig serial-chain discipline:** `PdfPigStatementFieldExtractor.cs` is the shared bottleneck (7 E2 stories touch it: S5/S6/S7/S8/S9/S10/S11). Advance ONE per round; parallelize only disjoint-project stories. `Directory.Packages.props` (S4 needs pHash NuGet) is also a serialize-point. Concurrent-edit collision DID occur this session (S8+S15 both touched JobVerdict.cs/AlertContext.cs) but merged coherently — VERIFIED by full integration build, not assumed. Watch for it.
- **Docker-gated DEFERRALS this wave:** S15 Testcontainers dup-alert IT (code-complete, unit-mocked). Prisma-E2 S1/S6/S7 + the E2E verification of S4/S5 are Docker/real-TCP-gated → not startable on this box.
- **NEXT Wave-1 REMAINING:** Veriqan-E2 S4(pHash,L,NuGet) S5(CL-35 Aptos) S6(CL-34 card) S7(CL-31 pagination) S9(timezone) S10(es-MX num) S11(CL-48 blank) S13(IVA→config) S16(dedup); Veriqan-COSMETIC S1/S2; Prisma-E2 S3(Sentinel trace) S5(ProcessId audit code) S8(CI docker) S4(Tesseract deadlock — needs OCR infra); Prisma-E5 cleanup S1-S5. Order: keep draining the PdfPig chain one-per-round + disjoint parallel.


**WAVE-0 TAIL — PRISMA TRACK COMPLETE + ADVERSARIALLY REVIEWED + REMEDIATED 2026-06-19.** Prisma W0 tail = PRISMA-E1 S6/S2/S7/S5 all done on `Liv`. HEAD=a250d72d.
Prisma-tail commits: 27b78e0c (S6 Serilog sinks — needed worker UseSerilog wire-up, brief's "config-only" premise was wrong), 56621a6e (S2 --migrate-only runner; workers only own PrismaDbContext, not 3 contexts), 03ae2a87 (S7 dashboard counts; Interlocked thread-safety fix), 9a36444e (S5 docker-compose.dev.yml 6-svc), a250d72d (S6/S7 REVIEW FIXES).
**Adversarial review (plan-completion-reviewer) caught 2 Majors → FIXED in a250d72d:** (1) `${SEQ_URL}`/`${OTLP_ENDPOINT}` are NOT interpolated by ASP.NET Core config — Serilog Seq sink crashed all 4 hosts at boot on unset var; fix = `%SEQ_URL%` (Serilog env-expansion) + OTLP Endpoint=null+env-override + per-host SEQ_URL default guard. (2) S7 endpoint test only asserted >=0; added `==3`-after-record endpoint test to Athena+Orion Worker.Tests. Verified: Athena.Worker.Tests 25/25, Reconciliator.Worker.Tests 8/8, Orion.Worker.Tests 24/25 (1 pre-existing env-gated SignalR `IngestionHubWireTests` timing fail — host boots fine), Web.UI 0/0.
**VERIQAN W0 TAIL — COMPLETE + ADVERSARIALLY REVIEWED + REMEDIATED 2026-06-19.** All 7 done on `Liv`:
V-S7 health (fe936377), V-S8 OTel/Serilog+spans (6fbba6f5), V-S10 extraction-coverage floor U2 (8cb842b5), V-S6 bundle move+guide (046f6905), V-S11 streaming batch + poison-PDF guards (d2dd7a82; orchestrator pre-commit fix removed a [ThreadStatic] anti-pattern), V-S3 Dockerfile/compose (7ae1bda4), V-S9 runbook (8b61d015).
**Owner ruling (V-S7) 2026-06-19:** Veriqan in-memory persistence = LIVE but NOT READY (/health/ready 503 when no durable DB) — resolves the S2-vs-S7 spec conflict, closes U4. In-memory still runs for dev (liveness 200).
**Veriqan-track adversarial review (plan-completion-reviewer) → 1 Major + 1 Minor FIXED (commit AFTER 8b61d015):** (Major) `Veriqan:BatchProcessor:MaxConcurrency` was dead config — BatchProcessor always used per-call BatchOptions (default 4); fixed so a non-default per-call value overrides, else the configured global drives consumer count. (Minor) runbook wrongly said ReprocessService is invoked by the pipeline — corrected (DI-registered only, not wired). Nits (stale tracker) addressed here.
**Wave-0 tail test gates (all re-verified independently):** Veriqan.Orchestration.Tests 63/63, Veriqan.Infrastructure.Extraction.Tests 137/137, Veriqan.Worker.HealthChecks.Tests 9/9, Veriqan.Application.Tests 115/115, ReferenceData.Tests 38/38, Veriqan.Worker builds 0/0. Prisma: Athena.Worker.Tests 25/25, Orion.Worker.Tests 24/25 (1 pre-existing env-gated SignalR fail), Reconciliator.Worker.Tests 8/8, dashboard HealthChecks 17/17 + 22/22, Web.UI 0/0.
**Docker-gated DEFERRALS (no Docker on box):** Prisma docker-compose.dev up + P-S2 Testcontainers migrate test + V-S3 docker build/compose up + Veriqan Testcontainers persistence ITs — all code-complete, run on a Docker/CI box to close. Live OTLP/Seq delivery also Docker-deferred.
**ENTIRE WAVE-0 TAIL (Prisma 4 + Veriqan 7 = 11 stories) DONE + both tracks reviewed. NEXT = owner steer: Wave 1 (cardinal/ops) per SPRINT-PLAN, or pause.**
**Lesson:** config-only stories that touch logging sinks MUST be verified by RUNNING a host (build ≠ boot) — `${}` vs `%%` and boot-crash were invisible to `dotnet build`.

---
### (prior) WAVE-0 CRITICAL-FEW COMPLETE + ADVERSARIALLY REVIEWED 2026-06-19 — 7/7 stories + 1 review fix, all committed, pushed, ground-truth-verified. HEAD=697b4fc8 on `Liv`.
Commits: 523737f1 (P-S3), 7160806a (V-S2), ebc7d644 (P-S4), 3957b3c6 (V-S1), ded95284 (P-S1), 61dd6371 (V-S5), 4ded1760 (V-S4), 697b4fc8 (review-fix #1).
**Adversarial review (plan-completion-reviewer):** confirmed ctor blast-radius safe (no prod path fails to resolve VerificationPipeline), re-verified 54/54 + 8/8. Found 1 Major → FIXED (697b4fc8: BLOCKED-from-binder verdicts now persisted; tests 56/56). Minors: #2/#3 no-action (out-of-scope simulator URLs / Docker-gated), #4 logged as follow-up (task: WAF test for Web.UI /health/ready 503 — wiring confirmed by code review, low priority).
**Deferred (no Docker on box):** P-S1 `docker build`, V-S5 Testcontainers `VerdictPersistenceIntegrationTests` — run on a Docker/CI box to fully close.
**NEXT options for owner:** (a) Wave-0 TAIL (PRISMA-E1 S2 auto-migration / S5 compose / S6 log sinks / S7 dashboard metrics; VERIQAN-E1 S3 Dockerfile+compose / S6–S11), (b) advance to Wave 1, or (c) pause. Awaiting owner steer.

### (prior) EXECUTION IN PROGRESS — 4 of 7 Owner approved Wave-0 critical few. Two parallel sequential tracks. Docker NOT available → Prisma-S1 `docker build` + Veriqan-S5 Testcontainers ACs are CODE-COMPLETE-but-E2E-DEFERRED.
- **DONE+committed+pushed+verified:** PRISMA-E1-S3 (523737f1), VERIQAN-E1-S2 (7160806a), PRISMA-E1-S4 (ebc7d644), VERIQAN-E1-S1 (3957b3c6). HEAD=3957b3c6 on `Liv`.
- **REMAINING:** PRISMA-E1-S1 (Dockerfiles), VERIQAN-E1-S5 (persist), then VERIQAN-E1-S4 (report/notify).
- **ENV INCIDENT (worked around):** the **Bash tool is DOWN** at harness level — `C:\Program Files\Git\cmd\git.exe` is missing (broken Git-for-Windows install), which also breaks Git-Bash's shell. **Workarounds in use:** (1) git via mingw git at `C:\Users\Abel Briones\AppData\Local\GitHubDesktop\app-3.5.4\resources\app\git\cmd\git.exe` driven from PowerShell — verified it sees a CLEAN tree (cygwin git at `C:\cygwin64\bin\git.exe` does NOT — it mangles line endings, do NOT use it for commits). (2) `dotnet` via PowerShell (`C:\Program Files\dotnet\dotnet.exe`, 10.0.301). (3) Grep/Glob/Read/Edit tools for search/edit (Bash-independent).

## Environment ground truth (verified 2026-06-19)
- Docker: **NOT running/available** → Testcontainers + `docker build` ACs cannot execute here.
- dotnet 10.0.301. Build/filesystem slow (E:). Build single projects, not the `.sln`.
- Confirmed paths:
  - Veriqan.Worker: `04 Services/Veriqan.Worker/` · csproj `ExxerCube.Prisma.Veriqan.Worker.csproj` · `Program.cs`
  - VerificationPipeline: `03 Orchestration/Veriqan.Orchestration/Pipeline/VerificationPipeline.cs`
  - Veriqan.Orchestration.Tests: `08 Tests/03 Orchestration/Veriqan.Orchestration.Tests/`
  - Veriqan persistence ITs: `08 Tests/05 System/Veriqan.Infrastructure.Persistence.IntegrationTests/`
  - Reconciliator: `04 Services/Reconciliator/Prisma.Reconciliator.Worker/`
  - Web.UI: `07 UI/UI/ExxerCube.Prisma.Web.UI/`
  - Athena.Worker / Orion.Worker confirmed under `04 Services/Athena|Orion/`

## Conflict map (why ordering is what it is)
- Veriqan `S2` ∩ `S5` → both edit `VeriqanOrchestrationExtensions.cs` ⇒ sequential.
- Veriqan `S1` ∩ `S2` → both edit Worker `Program.cs` ⇒ sequential.
- Veriqan `S5` ∩ `S4` → both edit `VerificationPipeline.cs::ProcessAsync` ⇒ sequential (persist=stage7 first, then report=8/notify=9).
- Prisma `S1` ∩ `S4` → both build Web.UI + Reconciliator ⇒ sequential.
- Prisma tree ⟂ Veriqan tree ⇒ the two tracks run in parallel.

## Execution order
**Track Prisma (sequential):** P-S3 (config) → P-S4 (health probes) → P-S1 (Dockerfiles)
**Track Veriqan (sequential):** V-S2 (appsettings+validation) → V-S1 (POST /verify) → V-S5 (persist stage) → V-S4 (report/notify stages)

## Story status
| Story | Track | Status | Verified by | Notes |
|-------|-------|--------|-------------|-------|
| PRISMA-E1-S3 config externalization | Prisma | ✅ DONE 523737f1 | git grep 0 + Web.UI build 0/0 | |
| PRISMA-E1-S4 real health probes | Prisma | ✅ DONE ebc7d644 | Reconciliator.Worker.Tests 8/8 (verified) | new HealthChecks proj mirrors Athena/Orion; 2 Tests.UI NavigationSmoke fail = env-gated (no SQL/Playwright), not regression |
| PRISMA-E1-S1 Dockerfiles + CI | Prisma | in_progress | dotnet build hosts (PS) | docker build = DEFERRED (no docker) |
| VERIQAN-E1-S2 appsettings+validation | Veriqan | ✅ DONE 7160806a | Orchestration.Tests 43/43 (verified) | |
| VERIQAN-E1-S1 POST /verify | Veriqan | ✅ DONE 3957b3c6 | Orchestration.Tests 45/45 (verified) | |
| VERIQAN-E1-S5 persist stage | Veriqan | in_progress | build (PS) | Testcontainers test = DEFERRED (no docker) |
| VERIQAN-E1-S4 report/notify stages | Veriqan | pending | build + NSubstitute tests | |

## Cadence
- Verify each story from ground truth (single-project `dotnet build` + relevant test) before marking done.
- Commit per cohesive story; push to `Liv`.
- Adversarial review at the track-completion boundary (skeptic pass vs the epics briefs) before declaring Wave-0 critical-few done.
