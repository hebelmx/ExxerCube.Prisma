{
  "_note": "Local developer overrides for ASPNETCORE_ENVIRONMENT=Development. Connection strings are intentionally empty here — supply them via env vars (ConnectionStrings__DefaultConnection / ConnectionStrings__ApplicationConnection), dotnet user-secrets, or a local .env file. See .env.example at the repo root.",
  "ConnectionStrings": {
    "DefaultConnection": "",
    "ApplicationConnection": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
